# MEMORY_ARCHIVE.md - Registro Histórico Completo de Acciones

Este archivo almacena el historial acumulativo e inmutable de **todas las acciones técnicas, operativas y arquitectónicas** tomadas en el proyecto "Promundo Sistema", categorizadas cronológicamente por fecha.

## 📅 2026-09-08 (Estabilización de Conexión Supabase Pooler IPv4 y Resolución de Error DNS ENOTFOUND en Alta de Terrenos)

### 🗄️ Infraestructura Cloud, Base de Datos & Alta de Terrenos
* **[ACC-084] [2026-09-08 02:30] Corrección de Prioridad de Conexión a Supabase Transaction Pooler (Puerto 6543) y Configuración Dual-Stack IPv4 en .env.local:** Diagnóstico y resolución mediante subagentes especializados (`clean-code`, `database-design`, `postgresql`, `nextjs-best-practices`). 1) **Causa Raíz:** Al agregar un nuevo terreno desde el modal de alta (`TerrenoCreateDialog`), la Server Action `createTerrenoAction` arrojaba `getaddrinfo ENOTFOUND db.rioosacxbuwkxntxmmpe.supabase.co`. Esto se debía a que en `src/db/index.ts` la cadena de conexión evaluaba `process.env.DIRECT_URL || process.env.DATABASE_URL`, priorizando la conexión directa `db.rioosacxbuwkxntxmmpe.supabase.co:5432`. Dado que Supabase asigna exclusivamente registros IPv6 (AAAA) para conexiones directas en tier gratuito, en entornos Windows/Node.js donde la red local no resuelve IPv6, la resolución DNS fallaba de inmediato. 2) **Resolución Arquitectónica:** En `src/db/index.ts`, se invirtió la prioridad para utilizar en primer lugar `process.env.DATABASE_URL` (Supabase Transaction Pooler en puerto 6543, compatible con `prepare: false` y con soporte dual-stack IPv4/IPv6 en `aws-0-sa-east-1.pooler.supabase.com`). En `.env.local`, se actualizó `DIRECT_URL` al Session Pooler de Supabase en puerto 5432 para garantizar soporte IPv4 total en cualquier script o migración de respaldo. 3) **Verificación:** Ejecución exitosa de `scripts/test-db-persistence.mjs` (inserción de terreno con PostGIS WGS84, consulta, actualización y eliminación limpia 100% PASS), ejecución de `scripts/check-db.mjs` (6 usuarios, 20 terrenos, 4 clientes recuperados), verificación de compilación y servidor de desarrollo activo en `http://localhost:3000` con respuesta HTTP 200 OK.

## 📅 2026-09-07 (Migración Integral a PostgreSQL, Erradicación de Mocks y Eliminación Fiduciaria de Terrenos)

### 🗄️ Infraestructura, Base de Datos & Erradicación de Mocks
* **[ACC-083] [2026-09-07 11:45] Purga Total y Definitiva de Archivos Mock, Sincronización Directa de Scripts Geoespaciales con PostgreSQL y Verificación Integral E2E:** Auditoría forense y saneamiento estructural mediante subagentes especializados (`clean-code`, `database-design`, `postgresql`, `lint-and-validate`). 1) **Purga Definitiva de `src/lib/mock`:** Eliminación física de los 6 archivos de seed estáticos (`auditoria-seed.ts`, `comisiones-seed.ts`, `documentos-seed.ts`, `negociaciones-seed.ts`, `parametros-normativos-seed.ts`, `terrenos-seed.ts`). Cero referencias o residuos mock en todo el árbol de código fuente. 2) **Desacoplamiento de Scripts de Auditoría:** Refactorización de `scripts/verify-spatial-integrity.mjs` para consultar dinámicamente la tabla `terrenos` en PostgreSQL/PostGIS mediante el pooler de Supabase (`process.env.DIRECT_URL || process.env.DATABASE_URL`), ejecutando las 20 comprobaciones espaciales y de vías directamente sobre datos fiduciarios persistidos (100% PASS, 20/20 lotes en tierra firme y en su trazado distrital). 3) **Dashboard UI:** Estandarización de indicador fiduciario en `src/app/page.tsx` a `"Persistencia de Datos: 100% PostgreSQL Real"`. 4) **Verificación Integral y Servidor Dev Permanente:** Ejecución de `npx tsc --noEmit` (0 errores), `npm run lint` (0 errores, 0 advertencias), ambas suites de auditoría (`verify-full-db-integrity.mjs` 11/11 PASS y `verify-spatial-integrity.mjs` 20/20 PASS), compilación completa de producción (`npm run build` 17/17 páginas exitosas) y servidor dev activo respondiendo HTTP 200 en las 13 rutas de la plataforma.
* **[ACC-082] [2026-09-07 09:54] Restauración y Diagnóstico Forense de la Pipeline CSS y Dev Server tras Build de Producción:** Diagnóstico y solución mediante subagentes especializados (`systematic-debugging`, `lint-and-validate`, `clean-code`). 1) **Causa Raíz de Pérdida de Estilos:** Tras ejecutar `npm run build` mientras el proceso `next dev` seguía activo, Next.js purgó y regeneró `.next` con la estructura hash de producción, suprimiendo los archivos CSS temporales de desarrollo (`.next/static/css/app/layout.css`). El proceso en memoria de `next dev` continuó solicitando la ruta de desarrollo inexistente arrojando HTTP 404, provocando que el navegador renderizara HTML plano sin estilos de Tailwind CSS. 2) **Resolución Limpia:** Terminación forzada de procesos Node residuales, purga completa del directorio de caché `.next` e inicio en limpio de `npm run dev` en segundo plano permanente como daemon. 3) **Verificación:** Inspección HTTP con PowerShell validando que `http://localhost:3000` entrega el HTML correcto y `http://localhost:3000/_next/static/css/app/layout.css` responde HTTP 200 OK con el bundle completo de Tailwind CSS (94 KB).
* **[ACC-080] [2026-09-07 08:55] Migración Fiduciaria Integral a PostgreSQL, Erradicación Total de Mocks y Sincronización en Producción:** Auditoría, refactor y verificación integral mediante subagentes especializados (`clean-code`, `database-design`, `postgresql`, `nextjs-app-router-patterns`, `react-patterns`, `lint-and-validate`). 1) **Sembrado Completo y Consistente en Supabase Cloud:** Ejecución exitosa de `scripts/seed-complete-db.mjs` poblando de forma relacional 8 tablas en PostgreSQL con PostGIS SRID 4326 (`usuarios`: 6, `propietarios`: 6, `terrenos`: 20 lotes referenciados, `clientes`: 4 constructoras, `documentos_terreno`: 15 documentos técnicos con CPUs, `negociaciones`: 10 deals en pipeline, `bitacora_negociacion`: 31 eventos inmutables de auditoría, `comisiones_cierres`: 2 liquidaciones fiduciarias). 2) **Auditoría Automatizada de Integridad:** Ejecución de `scripts/verify-full-db-integrity.mjs` con 11/11 verificaciones superadas (100% PASS, 0 fallos), validando conteos, integridad de claves foráneas y coordenadas geográficas. 3) **Erradicación Total de Mocks y Stores en Memoria:** Eliminación del 100% de imports desde `@/lib/mock` en toda la carpeta `src/`. Parámetros urbanísticos normativos migrados a constantes reales en `src/lib/constants/parametros-normativos.ts`. Purga de arrays volátiles en memoria Node.js en todos los servicios (`terrenos`, `negociaciones`, `documentos`, `comisiones`, `auth-service`, `configuracion`, `matching`). 4) **Arquitectura de Server Actions con Drizzle ORM:** Creación y unificación de acciones en `src/lib/actions/` (`terrenos-actions.ts`, `pipeline-actions.ts`, `documentos-actions.ts`, `comisiones-actions.ts`, `reportes-actions.ts`, `matching-actions.ts`, `auditoria-actions.ts`, `dashboard-actions.ts`). Conexión de todas las vistas (`/`, `/terrenos`, `/mapa`, `/pipeline`, `/documentos`, `/matching`, `/comisiones`, `/reportes`, `/auditoria`) a datos reales fiduciarios con invalidación de caché (`revalidatePath`). 5) **Validación y Build de Producción:** Resolución de tipado estricto (`npx tsc --noEmit` código 0), linter sin errores y compilación de producción exitosa (`npm run build` con 17/17 páginas estáticas y dinámicas compiladas). Servidor de desarrollo levantado y permanente según `AGENTS.md`.

### 🏗️ Módulo A: Inventario y Data Grid de Terrenos
* **[ACC-081] [2026-09-07 09:18] Incorporación del Catálogo Maestro Institucional de Zonificaciones Oficiales de Lima Metropolitana (MML / RATUS) y Expansión Integral en Selectores, Filtros y Matching:** Investigación previa con subagente especializado (`clean-code`, `typescript-pro`, `design-taste-frontend`, `lint-and-validate`). 1) **Normativa y Taxonomía Urbana de Lima:** Investigación basada en Ordenanza 1076-MML, RATUS (DS 012-2021-VIVIENDA) y parámetros distritales oficiales. Incorporación de 21 códigos canónicos agrupados en: Residenciales (`RDB`, `RDM`, `RDA`, `RDMA`, `VT`), Comerciales (`CV`, `CZ`, `CM`, `CI`, `CE`), Industriales (`I1`, `I2`, `I3`, `I4`), y Equipamientos / Especiales (`OU`, `ZRE`, `ZTE`, `E`, `H`, `ZRP`, `CH`). 2) **Catálogo Maestro Centralizado (`src/lib/constants/zonificaciones.ts`):** Creación del archivo canónico exportando `ZONIFICACIONES_LIMA`, `ZONIFICACIONES_CODES`, `ZONIFICACIONES_PRIORITARIAS_FILTRO`, `ZONIFICACIONES_POR_CATEGORIA`, `getZonificacionBadgeClass()` y `getZonificacionLabel()`. 3) **Selector en Alta de Lote (`TerrenoCreateDialog`):** Refactorización del dropdown de zonificación utilizando `SelectGroup` y `SelectLabel` por categorías ordenadas, permitiendo registrar predios con cualquiera de las zonificaciones oficiales (`RDMA`, `I2`, `RDM`, etc.) con nombres descriptivos. 4) **Filtros Dinámicos en Inventario (`TerrenosFilters`):** Ampliación del panel lateral de filtros con chips prioritarios y toggle expandible para acceder a las 21 zonificaciones con contador reactivo de selección. 5) **Ficha Técnica y Modos de Edición (`TerrenoDetailSheet`):** Conversión del campo de edición de zonificación a selector agrupado oficial y enriquecimiento del modo lectura con badges temáticos y tooltips normativos. 6) **Data Grid y Split View (`TerrenosColumns` & `TerrenosSplitList`):** Estandarización de badges corporativos con paleta pastel Bloomberg Light. 7) **Motor de Matching (`matching.ts`):** Calibración de absorción residencial e industrial reconociendo `RDMA` e `I2`. 8) **Validación:** 0 errores TypeScript (`npx tsc --noEmit`), 0 advertencias en ESLint (`npm run lint`), respuestas HTTP 200 OK y servidor de desarrollo activo.
* **[ACC-079] [2026-09-07 00:25] Implementación de Eliminación Fiduciaria Segura de Terrenos con Modal de Confirmación y Reactividad Multi-Módulo:** Diagnóstico y solución mediante subagentes especializados (`clean-code`, `react-patterns`, `nextjs-best-practices`, `postgresql`, `lint-and-validate`). 1) **Causa Raíz:** No existía mecanismo en la aplicación para eliminar terrenos del catálogo institucional. El usuario solicitó explícitamente agregar la opción en el modal de ficha técnica bajo los botones de mapa. 2) **Server Action con Blindaje Fiduciario:** Creación de `deleteTerrenoAction` en `src/lib/actions/terrenos-actions.ts`. Incluye pre-flight check sobre la tabla `negociaciones`: si el terreno tiene negociaciones vinculadas en el Pipeline Comercial (`ON DELETE RESTRICT`), la acción bloquea la eliminación con una advertencia en español protegiendo la auditoría de bitácoras y comisiones. Si no tiene negociaciones, ejecuta una transacción atómica eliminando documentos asociados (`documentos_terreno`) y el lote de terreno, preservando al titular en `propietarios`. Invalida caché de Next.js (`/terrenos`, `/mapa`, `/`, `/pipeline`, `/documentos`). En `terrenos.ts`, adición de `deleteTerrenoEnMemoria` para consistencia. 3) **Botón Destructivo y Diálogo Fiduciario Bloomberg Light en Ficha Técnica (`TerrenoDetailSheet`):** Incorporación del botón "Eliminar Terreno" con estilo Bloomberg Light (`bg-rose-50 text-rose-700 hover:bg-rose-100 border border-rose-200`) ubicado en la pestaña *Ficha Técnica* inmediatamente bajo los botones de mapas (`Ver en Mapa Geoespacial del Sistema` y `Abrir en Google Maps Externo`). Despliegue de un diálogo modal (`Dialog` de Radix UI / Bloomberg Light) con resumen del activo (código, dirección, área, precio, documentos afectados), advertencia clara si tiene negociaciones bloqueantes, checkbox obligatorio de confirmación fiduciaria y botón de confirmación con spinner `Loader2`. 4) **Sincronización Reactiva Multi-Módulo:** Incorporación del prop `onTerrenoDeleted?: (terrenoId: string) => void` en `TerrenoDetailSheet`. En `TerrenosClient`, filtrado inmediato de `terrenosList` y limpieza de selección. En `MapaClient`, habilitación de `setTerrenosList`, sincronización con `initialTerrenos` y filtrado reactivo. En `DocumentosClient`, limpieza de selección y recarga de documentos. 5) **Verificación Automatizada y E2E:** Ejecución de `node scripts/test-delete-terreno.mjs` validando inserción, cascade en documentos, bloqueo preventivo con negociaciones activas y purga limpia en PostgreSQL (100% PASS). Compilación estricta con `npx tsc --noEmit` (0 errores), linter con `npm run lint` (0 errores, 0 advertencias) y verificación HTTP 200 OK en `/terrenos`, `/mapa` y `/documentos`.
* **[ACC-078] [2026-09-07 00:05] Solución Integral de Visibilidad Inmediata y Reactividad en Alta de Terrenos (TerrenoCreateDialog & TerrenosClient):** Diagnóstico y solución mediante subagentes especializados (`systematic-debugging`, `react-patterns`, `nextjs-best-practices`, `lint-and-validate`). 1) **Causa Raíz de Desaparición Aparente:** En `TerrenosClient`, el estado de TanStack Table tenía configurado un ordenamiento fijo alfabético por código (`sorting: [{ id: "codigoInterno", desc: false }]`) y paginación fija en 15 filas (`pageSize: 15`). Al registrar un nuevo terreno (ej. `TR-SURC-020` o `TR-SURC-021`), aunque `createTerrenoAction` lo insertaba correctamente en Supabase PostgreSQL con PostGIS y `handleCreateTerreno` lo agregaba en el índice 0 del estado local (`setTerrenosList([nuevo, ...prev])`), TanStack Table aplicaba `getSortedRowModel()`, reordenando todo alfabéticamente. Dado que el código alfabético de Surco o San Isidro quedaba en la posición 17-18, el registro era desplazado a la **Página 2** del Data Grid. Como la tabla permanecía en la Página 1, el usuario no veía el terreno y asumía que no se había guardado. Asimismo, el `useEffect` de sincronización con `searchParams` dependía de `terrenosList`, re-aplicando filtros de URL obsoletos tras la inserción, y si el usuario tenía filtros distritales o de texto activos, el nuevo terreno era excluido de `filteredTerrenos`. Además, en `TerrenoCreateDialog`, la generación de código sugerido podía colisionar con códigos existentes de seed (ej. `TR-SISI-019`), y los errores de validación no realizaban scroll hacia la alerta en el formulario. 2) **Ordenamiento Natural y Paginación Fila 1:** En `TerrenosClient`, se cambió el ordenamiento inicial a `sorting: []` (respetando el orden natural de la base de datos `orderBy: [desc(terrenos.createdAt)]`, más recientes primero). En `handleCreateTerreno`: se limpian los filtros acumulativos (`setFiltros({})`), se restablece el ordenamiento (`setSorting([])`), se fuerza la tabla a la primera página (`table.setPageIndex(0)`), se añade al inicio del listado y se resalta la fila (`setSelectedTerreno`). 3) **Banner de Confirmación Bloomberg Light:** Incorporación de micro-banner corporativo (`bg-emerald-50/90 border-emerald-200 text-emerald-800`) sobre el Data Grid notificando que el lote fue creado exitosamente y posicionado en la primera fila, con botón rápido "Abrir Ficha Técnica". 4) **Unicidad de Código Sugerido y Auto-Scroll en Diálogo:** En `TerrenoCreateDialog`, se incorporó `codigosExistentes` para que el generador correlativo detecte códigos ocupados e incremente el correlativo automáticamente hasta encontrar uno libre (garantizando 0 colisiones en PostgreSQL). Se añadió `formRef` con auto-scroll suave hacia el mensaje de error en caso de fallo, y reseteo completo del formulario (`resetForm()`) tras guardar con éxito. En `createTerrenoAction`, cálculo defensivo de `precioM2` y captura amigable de violación de clave única `23505`. 5) **Sincronización Server-Client & Desacoplamiento de URL:** Se implementó `terrenosListRef` para desacoplar el efecto de `searchParams` de las mutaciones de inventario, y `useEffect` para sincronizar `initialTerrenos` e `initialPropietarios` ante revalidaciones de Next.js. 6) **Verificación:** `npx tsc --noEmit` completado con 0 errores, `npm run lint` con 0 advertencias y 0 errores, servidor dev activo en puerto 3000 con HTTP 200 OK.

## 📅 2026-09-06 (Control de Accesos Google OAuth, Supabase Cloud, PostGIS, Alta de Lotes y Publicación GitHub)

### 💼 Pipeline de Negociaciones & Sincronización de Usuarios Reales
* **[ACC-077] [2026-09-06 21:15] Blindaje Definitivo de Selección de Broker en DealCreateDialog y Restauración de Servidor Dev Permanente:** Diagnóstico y solución mediante subagentes especializados (`clean-code`, `react-patterns`, `nextjs-best-practices`, `lint-and-validate`). 1) **Causa Raíz de Reversión de Broker:** En `DealCreateDialog`, el `useEffect` de broker dependía de `[isOpen, currentUserId, brokers]` sin bandera de apertura. Al seleccionar otro broker, la resolución asíncrona de `currentUser` o re-renders de `PipelineClient` disparaban nuevamente el efecto, ejecutando `setBrokerId(currentUserId)` y forzando la selección de regreso a Paulo Salem. 2) **Blindaje con Flag de Ciclo de Vida (`hasInitializedRef`):** Se implementó una referencia mutable (`hasInitializedRef`) que solo inicializa al usuario por defecto una única vez cuando el modal pasa de cerrado a abierto. Una vez abierto, cualquier selección manual de broker en el dropdown se preserva de forma inmutable sin ser sobreescrita por re-renders. 3) **Estabilización de DealDetailSheet y PipelineClient:** En `DealDetailSheet`, se agregó `prevDealIdRef` para evitar reseteos de broker en bitácora durante la inspección del mismo deal. En `PipelineClient`, se removió la llamada redundante a `setBrokers` tras el montaje. 4) **Restauración y Verificación del Servidor Next.js:** El servidor de desarrollo fue levantado en segundo plano en `http://localhost:3000`. Se verificó que todos los chunks JS/CSS de `_next/static` responden HTTP 200 OK tanto en `/` como en `/login` y `/pipeline`. 5) **Validación:** 0 errores en TypeScript (`npx tsc --noEmit`), 0 errores y advertencias en ESLint (`npm run lint`).
* **[ACC-076] [2026-09-06 20:58] Estabilización de Ciclos de Renderizado en PipelineClient y Blindaje contra Congelamiento de UI:** Diagnóstico y solución mediante subagentes especializados (`clean-code`, `react-patterns`, `nextjs-best-practices`). 1) **Causa Raíz:** En `PipelineClient`, la función `loadData` incluía `selectedDeal` como dependencia en `useCallback` y al mismo tiempo llamaba a `setSelectedDeal(refreshed)`. Además, un `useEffect` sincronizaba `searchParams` y `deals`, actualizando `filtros` y `selectedDeal` cada vez que `deals` mutaba. Al recargar la página `/pipeline`, esta cascada de dependencias circulares provocaba ejecuciones continuas de re-renderizado, bloqueando el hilo principal de React en el navegador y provocando pantalla en blanco tras la recarga. 2) **Estabilización con Mutable Ref:** Se desacopló `selectedDeal` del `useCallback` de `loadData` utilizando `selectedDealRef`, garantizando que `loadData` solo se regenere cuando cambien los `filtros`. 3) **Separación de Efectos de URL:** Se dividió el efecto de `searchParams` en dos efectos específicos: uno para sincronizar filtros de URL y otro para resolver la preselección inicial del deal solicitado. 4) **Verificación:** `npx tsc --noEmit` completado con 0 errores, `npm run lint` con 0 errores y 0 advertencias, y servidor de desarrollo respondiendo HTTP 200 OK.
* **[ACC-075] [2026-09-06 20:51] Corrección de Retención de Selección de Broker en DealCreateDialog y Desacoplamiento de Ciclo de Vida Reactivo:** Diagnóstico y solución mediante subagentes especializados (`clean-code`, `react-patterns`, `nextjs-best-practices`). 1) **Causa Raíz:** En el modal de alta de negociaciones (`DealCreateDialog`), al seleccionar un broker diferente (ej. cualquier broker de la lista desplegable), el selector volvía de inmediato a "Paulo Salem". Esto ocurría porque un `React.useEffect` incluía `[brokers, currentUserId, isOpen, brokerId]` como dependencias y ejecutaba `if (currentUserId && brokers.some((b) => b.id === currentUserId)) setBrokerId(currentUserId);`. Cada vez que el usuario seleccionaba otro broker, cambiaba `brokerId`, disparando nuevamente el efecto y sobreescribiendo la selección con `currentUserId`. 2) **Desacoplamiento Reactivo:** Se refactorizó el `useEffect` para depender estrictamente de `[isOpen, currentUserId, brokers]`, inicializando al usuario actual únicamente al abrir el diálogo o si se cargan los brokers por primera vez. Una vez abierto el modal, la selección manual del usuario (`setBrokerId`) se preserva de forma inmutable hasta el envío del formulario o cierre del modal. 3) **Auditoría Preventiva en Modales Conexos:** Se auditó y optimizó `DealDetailSheet` para evitar comportamientos análogos en la bitácora con `selectedBrokerId`. 4) **Verificación:** `npx tsc --noEmit` completado con 0 errores, `npm run lint` con 0 errores y 0 advertencias, y servidor de desarrollo activo.
* **[ACC-074] [2026-09-06 19:55] Sincronización Integral de Usuarios Reales de Base de Datos (Brokers) en Pipeline Comercial y Erradicación de Mocks:** Investigación forense previa con subagente especializado (`clean-code`, `react-patterns`, `nextjs-app-router-patterns`). 1) **Causa Raíz:** En el modal de alta de negociaciones (`DealCreateDialog`), el selector de "Broker Responsable" listaba nombres hardcodeados (`Alvaro Barrenechea`, `Sofia Mendoza`, `Diego Palacios`) provenientes de `mockUsuarios` en `negociaciones-seed.ts`. Además, en `createNegociacion`, `updateEtapaNegociacion` y `addBitacoraEvento` existía un fallback que forzaba el ID a `"usr-01"`. 2) **Server Action de Obtención de Usuarios Reales:** Creación de `src/lib/actions/pipeline-actions.ts` con `getBrokersAction()`, consultando directamente en PostgreSQL/Supabase a los usuarios con `estadoAcceso = 'aprobado'` y `activo = true` (roles `admin`, `broker_senior`, `broker_junior`), ordenados alfabéticamente y con fallback de respaldo vía `authService.getUsuariosSolicitudes()`. 3) **Hidratación Servidor -> Cliente:** En `src/app/pipeline/page.tsx` (Server Component), precarga de `brokers` vía `getBrokersAction()` e inyección como `initialBrokers` en `PipelineClient`. En `PipelineClient`, estado reactivo sincronizado con `setBrokersStore()` y detección de sesión activa (`getCurrentUserSession()`). 4) **Experiencia de Usuario en `DealCreateDialog`:** Selector enriquecido con microbadges corporativos de rol (`admin`, `broker_senior`, `broker_junior`) bajo estándar estricto Bloomberg Light Theme, preselección automática del usuario logueado o primer broker real disponible, y eliminación absoluta de dependencias a `"usr-01"`. 5) **Blindaje Defensivo contra Nulos:** Incorporación de optional chaining y valores seguros en `deal-card.tsx` (`brokerNombre`, iniciales), `deal-detail-sheet.tsx` y `pipeline-table-view.tsx` para evitar caídas en pantalla si se visualizan deals históricos. 6) **Verificación:** 0 errores TypeScript (`npx tsc --noEmit`), 0 advertencias ESLint (`npm run lint`), build de producción aprobado (17/17 páginas generadas exitosamente) y respuesta HTTP 200 OK en `/pipeline`.

### 🗺️ Catastro Geoespacial & Consistencia Territorial WGS84
* **[ACC-073] [2026-09-06 19:15] Calibración Catastral Oficial de Bounding Boxes de Lima y Corrección de Falso Positivo Geoespacial en Miraflores Este:** Diagnóstico y solución mediante subagentes especializados (`clean-code`, `react-patterns`, `nextjs-best-practices`). 1) **Causa Raíz:** Al registrar un lote de suelo en "Gustavo Escudero 218" (Calle Teniente Coronel G. Escudero O., Miraflores, coords `[-12.1260165, -77.005606]`), el sistema bloqueaba el guardado arrojando `Alerta de Georreferenciación: La coordenada [-12.1260165, -77.005606] se encuentra fuera de los límites catastrales de Miraflores. Rango permitido Lat [-12.138, -12.108], Lng [-77.042, -77.018]`. Ocurría porque la definición previa en `geo-validator.ts` establecía un límite oriental arbitrario (`maxLng: -77.0180`), excluyendo erróneamente todo el sector este de Miraflores (zona Alexander Von Humboldt, La Aurora este, Benavides cdras 20-30 y Tomás Marsano hasta `-77.001`). 2) **Calibración Oficial de Bounding Boxes:** En `src/lib/map/geo-validator.ts` y `scripts/verify-spatial-integrity.mjs`, se actualizaron los 15 bounding boxes distritales de Lima Metropolitana contrastándolos con las coordenadas administrativas oficiales de OpenStreetMap/IGN e incorporando un margen de tolerancia de 500m (`tolerance = 0.005`) para cubrir avenidas limítrofes. Miraflores se calibró a `Lat [-12.1450, -12.1000], Lng [-77.0600, -76.9950]`, permitiendo el registro fluido de Gustavo Escudero y vías circundantes. 3) **Protección Costera y UX Asistida:** En `isCoordinateInPacificOcean`, se blindó la cota de San Miguel a `-77.094` para evitar falsos positivos en el Malecón Costanera. En `validateTerrenoCoordinates`, se implementó detección predictiva de distritos colindantes para asistir al operador en caso de desviaciones reales. 4) **Verificación:** 18/18 PASS en `scripts/verify-spatial-integrity.mjs`, validación unitaria de las coordenadas del usuario `{ valid: true }`, `npx tsc --noEmit` completado con 0 errores y `npm run lint` con 0 advertencias.

### 🛠️ Arquitectura & Límites Cliente-Servidor Next.js
* **[ACC-072] [2026-09-06 16:08] Aislamiento Estricto de Límites Cliente-Servidor (Resolución de Error 'net' en Next.js App Router):** Diagnóstico y solución mediante subagentes especializados (`nextjs-app-router-patterns`, `nextjs-best-practices`, `clean-code`). 1) **Causa Raíz:** Al navegar a `/terrenos`, Next.js arrojaba `Build Error: Module not found: Can't resolve 'net'` en `./node_modules/postgres/src/connection.js`. Ocurría porque componentes cliente (`"use client"`: `src/app/terrenos/terrenos-client.tsx`, `src/components/terrenos/terreno-detail-sheet.tsx` y `src/app/documentos/documentos-client.tsx`) importaban funciones de `src/lib/services/terrenos.ts`, el cual importa `@/db` y el driver nativo de Node.js `postgres`. Webpack intentaba empaquetar el módulo de red de Node (`net`) en el bundle del navegador. 2) **Desacoplamiento Arquitectónico:** Creación de Server Actions con directiva `"use server"` en `src/lib/actions/terrenos-actions.ts`: `createPropietarioAction`, `getTerrenosAction` y `getTerrenoByIdAction`. En `terrenos-client.tsx`: erradicación de imports de `services/terrenos` reemplazándolos por `createPropietarioAction`. En `terreno-detail-sheet.tsx`: importación directa de `calcularMatchingTerreno` desde `src/lib/services/matching.ts` (módulo de cálculo puro sin acoplamiento a base de datos). En `documentos-client.tsx`: reemplazo de `getTerrenos` por `getTerrenosAction`. Con esto, `src/lib/services/terrenos.ts` queda reservado estrictamente para Server Components (`/terrenos/page.tsx` y `/mapa/page.tsx`). 3) **Verificación:** `npx tsc --noEmit` completado con 0 errores, `npm run lint` completado con 0 advertencias y 0 errores. Pruebas HTTP contra el servidor dev en `/terrenos`, `/mapa`, `/documentos`, `/pipeline` y `/matching` respondiendo HTTP 200 OK y 0 errores de compilación Webpack.

### 🗺️ Módulo A & Infraestructura: Persistencia Real en Base de Datos y Mini-Mapa con Geocodificador
* **[ACC-071] [2026-09-06 10:45] Persistencia Real en Base de Datos PostgreSQL/PostGIS (Supabase) y Selector de Ubicación en Mini-Mapa con Buscador de Dirección para Alta de Terrenos:** Investigación exhaustiva previa mediante subagentes especializados (`postgresql`, `nextjs-app-router-patterns`, `nextjs-best-practices`, `react-patterns`, `clean-code`). 1) **Causa Raíz Diagnosticada:** La base de datos Supabase (`rioosacxbuwkxntxmmpe`) contaba con 0 registros en `terrenos` y `propietarios`, y la aplicación leía/escribía en arrays volátiles en memoria Node.js (`memoryTerrenos`), lo que provocaba que nuevos registros y cambios web desaparecieran al refrescar. Además, el alta requería escribir manualmente latitud y longitud. 2) **Siembra e Infraestructura de Base de Datos:** Creación y ejecución de `scripts/seed-terrenos.mjs`: sembrados 18 terrenos con geometrías PostGIS `ST_SetSRID(ST_MakePoint(lng, lat), 4326)`, 5 propietarios, 4 constructoras y 3 usuarios en Supabase PostgreSQL. En `src/db/index.ts`: conexión con SSL requerido (`ssl: 'require'`) y configuración adecuada para el Connection Pooler de Supabase en puerto 6543 (`prepare: false`). En `src/db/schema/custom-types.ts`: soporte en `postgisGeometryPoint.fromDriver` para decodificar tanto WKT (`POINT(x y)`) como EWKB Hexadecimal retornado por consultas de PostGIS. 3) **Server Actions con Persistencia Inmutable:** Creación de `src/lib/actions/terrenos-actions.ts` con `createTerrenoAction` y `updateTerrenoAction`, validación geoespacial territorial metropolitana (`validateTerrenoCoordinates`), inserción de puntos PostGIS con `ST_SetSRID(ST_MakePoint(...), 4326)` e invalidación de caché de Next.js (`revalidatePath`). Refactor de `src/lib/services/terrenos.ts` para consultar directamente Supabase PostgreSQL vía Drizzle ORM (`db.query.terrenos.findMany` con relaciones). 4) **Geocodificador y Proxy de OpenStreetMap:** Creación del endpoint `/api/geocode/route.ts` con proxy a OSM Nominatim acotado a Lima Metropolitana (`countrycodes=pe`, bounding box `[-77.25,-12.35,-76.75,-11.75]`), caché por hora y User-Agent fiduciario. Inclusión en `src/middleware.ts` para evitar redirecciones al login. 5) **Componente de Selector de Ubicación (`TerrenoLocationPicker`):** Desarrollo de `src/components/terrenos/terreno-location-picker.tsx` con mini-mapa interactivo de 220px en MapLibre GL JS (estilo Positron claro, sin fondos oscuros), buscador de dirección con sugerencias predictivas desplegables, pin arrastrable personalizado (`cursor-grab`) y posicionamiento instantáneo al hacer clic en el mapa. Indicador visual dinámico de validación territorial metropolitana (badge verde "Ubicación en tierra firme" o alerta roja en caso de caer fuera del distrito o en el mar). 6) **Integración en Modal de Alta (`TerrenoCreateDialog`):** Integración del `TerrenoLocationPicker`, remoción de la obligación de digitar números de latitud/longitud, autocompletado de dirección y distrito, y persistencia fiduciaria vía `createTerrenoAction`. 7) **Edición y Actualización sin Rebote (`TerrenoDetailSheet` y `TerrenosClient`):** En `src/components/terrenos/terreno-detail-sheet.tsx`: selector interactivo de estado comercial (`Disponible`, `En Negociacion`, `Vendido`, `Reservado`, `Inactivo`) y panel editable de parámetros urbanísticos/financieros persistiendo en PostgreSQL vía `updateTerrenoAction`. En `src/app/terrenos/terrenos-client.tsx`: callback `onTerrenoUpdated` reactivo en tiempo real. 8) **Verificación Automatizada:** `node scripts/test-db-persistence.mjs`: inserción, consulta PostGIS, modificación de estado/precio y eliminación exitosa contra Supabase Cloud PostgreSQL. `node scratch/test-e2e.mjs`: geocodificación de avenidas de Lima funcionando con coordenadas exactas. `npx tsc --noEmit`: 0 errores de tipado. `npm run lint`: 0 advertencias. Servidor de desarrollo `localhost:3000` activo y respondiendo.

### 🔐 Seguridad, Autenticación y Control de Accesos
* **[ACC-070] [2026-09-06 02:13] Limpieza Institucional de la Vista de Login (/login):** Remoción de la insignia técnica de desarrollo `"SISTEMA v2.5"` y erradicación definitiva de la sección y botones de `"ACCESO RÁPIDO & PRUEBAS"` (simulación de superadministrador y de postulante) en `src/app/login/login-client.tsx`. Limpieza de imports residuales no utilizados (`ShieldCheck`, `UserPlus`, `ExternalLink`, `simularLoginAction`). El formulario queda 100% sobrio y corporativo, enfocado exclusivamente en la acción de "Continuar con Google" y el protocolo fiduciario institucional. Verificación con `npm run build` exitoso (0 errores, 17/17 páginas compiladas) y servidor de desarrollo activo.
* **[ACC-069] [2026-09-06 01:45] Implementación Integral de Control de Accesos con Google OAuth, Flujo de Cuarentena en Espera, Módulo Administrativo /accesos y Superadministrador Inmutable paulosalem8@gmail.com:** Investigación profunda previa mediante subagentes especializados (`clean-code`, `nextjs-best-practices`, `nextjs-app-router-patterns`, `database-design`, `postgresql`). 1) **Base de Datos & DDL:** Creación de enum `estado_acceso` (`pendiente`, `aprobado`, `denegado`), extensión de tabla `usuarios` en Drizzle y Supabase PostgreSQL (`auth_id`, `avatar_url`, `estado_acceso`, `fecha_solicitud`, `fecha_resolucion`, `resuelto_por`, `notas`), creación de índices y siembra inmutable de `paulosalem8@gmail.com` como Superadmin aprobado. 2) **Página de Login (`/login`):** Interfaz corporativa Bloomberg Light Theme (`bg-white border-slate-200`) con botón oficial Google Sign-In SVG conectado a `@supabase/ssr` y aviso fiduciario de cuarentena. 3) **Pantalla de Solicitud en Revisión (`/espera`):** Vista de espera institucional para nuevos usuarios que ingresan con Google, mostrando datos de la cuenta postulante, estado pendiente y botón reactivo de verificación de aprobación. 4) **Módulo Administrativo de Gestión de Accesos (`/accesos`):** Vista de alta densidad para administradores con KPIs en tiempo real (solicitudes pendientes, brokers activos, denegados), pestañas de filtrado, Data Grid compacto con selector de rol (`broker_junior`, `broker_senior`, `admin`), botones de acción directa (`Aprobar Acceso`, `Denegar`, `Revocar`) y protección inmutable con candado para `paulosalem8@gmail.com`. 5) **Desacoplamiento de Layout & Middleware:** Creación de `AppShell` para aislar `/login` y `/espera` de la Sidebar institucional, y `middleware.ts` para blindar el perímetro de todas las rutas operativas. Integración de badge reactivo en Sidebar y campana en Header. 6) **Verificación:** 0 errores TypeScript (`npx tsc --noEmit`), 0 advertencias ESLint (`npm run lint`), build de producción aprobado (17/17 rutas estáticas y dinámicas compiladas exitosamente) y endpoints verificados en el servidor dev.

### 🎨 Diseño y Experiencia de Usuario (UI/UX)
* **[ACC-068] [2026-09-06 01:25] Eliminación de Etiquetas de Módulos (Mod A, Mod B, Mod C, etc.) en Sidebar y Filtros de Auditoría:** Investigación y auditoría visual de componentes (`clean-code` y `react-patterns`). 1) **Limpieza en Sidebar (`src/components/layout/sidebar.tsx`):** Remoción de los badges estáticos `"Mod A"`, `"Mod B"`, `"Mod C"`, `"Mod D"`, `"Mod E"`, `"Mod F"`, `"Mod G"`, `"Mod H1"`, `"Mod H2"` de todos los elementos de navegación de `navigationItems`, tanto en su renderizado en modo expandido como en el `TooltipContent` flotante en modo colapsado, ofreciendo una apariencia limpia, pulida y 100% orientada al usuario de negocio institucional. 2) **Limpieza en Filtros de Auditoría (`src/components/auditoria/auditoria-toolbar.tsx`):** Eliminación de los sufijos identificadores internos `(Mod A)`, `(Mod C)`, `(Mod D)`, `(Mod E)`, `(Mod F)`, `(Mod G)` y `(Mod H1)` en el array de opciones `modulosDisponibles`, estandarizando los nombres formales de cada módulo del sistema (`"Terrenos"`, `"Documentos & CPU"`, `"Pipeline Negociaciones"`, `"Motor de Matching"`, `"Comisiones & Finanzas"`, `"Métricas BI"`, `"Configuración Normativa"`). 3) **Verificación:** 0 errores en TypeScript (`npx tsc --noEmit`), 0 advertencias en ESLint (`npm run lint`), build de producción intacto y sincronización en GitHub.

### 🏗️ Módulo A: Inventario y Data Grid de Terrenos
* **[ACC-067] [2026-09-06 01:21] Implementación Integral del Alta de Nuevo Lote de Suelo Institucional (TerrenoCreateDialog) y Corrección de DialogTitle Light Theme:** Investigación previa mediante subagente especializado de dominio inmobiliario y arquitectura frontend (`clean-code`, `react-patterns`, `nextjs-app-router-patterns`). 1) **Detección de Causa Raíz:** El botón "+ Nuevo Lote" en `src/components/terrenos/terrenos-toolbar.tsx` se encontraba estático sin prop `onClick`, y la vista cliente `TerrenosClient` no contaba con diálogo ni estado para registrar activos. 2) **Corrección de DialogTitle en Tema Claro:** En `src/components/ui/dialog.tsx`, corrección de la clase residual `text-white` en `DialogTitle` reemplazándola por `text-slate-900` para garantizar legibilidad absoluta sobre el fondo corporativo blanco `bg-white` y cabecera `bg-slate-50`. 3) **Creación de TerrenoCreateDialog:** Desarrollo del componente `src/components/terrenos/terreno-create-dialog.tsx` con soporte para: autogeneración de código institucional de cartera (`TR-[DIST]-[SEQ]`) editable; alta rápida inline de propietarios titulares en SUNARP con DNI/RUC; selector de distritos metropolitanos de Lima con cálculo automático de coordenadas centroides referenciales; validación geoespacial en tiempo real con `validateTerrenoCoordinates` para prevenir coordenadas erróneas o marítimas en el Océano Pacífico; parámetros urbanísticos (zonificación RDA/RDM/CZ/CM/I1/ZTE, área m², frente lineal, fondo calculado, altura en pisos y usos permitidos); y cálculo bidireccional reactivo entre precio total (USD) y precio por metro cuadrado ($/m²). 4) **Integración de Servicios y Estado Reactivo:** Extensión de `src/lib/services/terrenos.ts` con `getPropietarios()`, `createPropietarioEnMemoria()` y `createTerrenoEnMemoria()`. En `src/app/terrenos/terrenos-client.tsx`, conexión del disparador `onNuevoLote`, actualización reactiva inmediata de la tabla (`terrenosList`, contadores de toolbar y selección con apertura automática en el sheet de inspección). Carga paralela de `initialPropietarios` en `src/app/terrenos/page.tsx`. 5) **Validación y Calidad:** 0 errores en TypeScript (`npx tsc --noEmit`), 0 advertencias en ESLint (`npm run lint`), build de producción aprobado (`npm run build` con 13/13 rutas estáticas generadas) y verificación HTTP 200 OK con el servidor de desarrollo activo.

### 🗄️ Infraestructura Cloud & Persistencia de Base de Datos
* **[ACC-066] [2026-09-06 01:18] Vinculación de Remoto GitHub y Push Inicial Exitoso de Rama Main:** Configuración de origen remoto `https://github.com/salempc-pe/central-promundo.git` y sincronización upstream `git push -u origin main` completada con éxito. Código fuente, esquemas Drizzle, configuración Next.js 14 y suites de validación 100% disponibles en GitHub con exclusión estricta de variables de entorno y credenciales sensibles (`.env.local`).
* **[ACC-065] [2026-09-06 01:16] Conexión Integral de Supabase PostgreSQL + PostGIS, Ejecución de Migración DDL y Validación de Build de Producción:** Configuración de `.env.local` con credenciales de Supabase (`rioosacxbuwkxntxmmpe`). Ejecución exitosa de migración DDL (`scripts/apply-migration.mjs` y `0000_init_postgis_schema.sql`) habilitando extensiones `postgis` y `uuid-ossp`, 8 tablas (`terrenos`, `documentos_terreno`, `propietarios`, `clientes`, `negociaciones`, `bitacora_negociacion`, `comisiones_cierres`, `usuarios`), triggers para cálculo de punto espacial WGS84 e índices GiST y B-Tree. Inicialización de Git con rama `main`, blindaje de credenciales en `.gitignore`, verificación de `npm run build` (13/13 rutas estáticas compiladas exitosamente con 0 errores) y commit inicial para despliegue en Vercel.

## 📅 2026-09-05 (Optimización de Arrastre a 60fps en MapLibre, Recalibración Geoespacial y Rediseño de Popup HUD)

### 🗺️ Módulo B: Mapa Geoespacial & Catastro de Terrenos
* **[ACC-064] [2026-09-05 19:55] Erradicación de la Sensación de Vista 3D (Transición a 2D Planar con easeTo) y Desacoplamiento de Clic en Marcador vs Sheet Lateral:** Investigación forense de cinemática de cámara MapLibre (`systematic-debugging` y `uxui-principles`). 1) **Causa de la Sensación 3D:** El método `map.flyTo` implementa el algoritmo de van Wijk (2003) de vuelo parabólico espacial (zoom-out para ganar altitud y swoop dive en picada); combinado con el Field of View ($\approx 37^\circ-53^\circ$) de la proyección WebGL, genera un campo de flujo óptico con distorsión periférica que el ojo percibe idéntico a un descenso inclinado desde un costado ("efecto de vista 3D"). Reemplazo de `map.flyTo` por `map.easeTo` (`zoom: 16.0`, `duration: 500ms`, `easing: quad ease-out`), logrando un desplazamiento y zoom 100% planos sobre el plano 2D perpendicular (vista cenital pura sin swoop ni cambio de altitud). Incorporación de `linear: true` en todas las llamadas a `map.fitBounds` (cargas y auto-encuadre). 2) **Desacoplamiento de Interacción UX:** En `terrenos-map.tsx` y `mapa-client.tsx`, se desacopló el clic del marcador de la apertura del Sheet. Anteriormente, pulsar un pin forzaba de inmediato la apertura del Sheet drawer de 672px con un backdrop oscuro (`fixed inset-0 bg-slate-800/40 backdrop-blur`), oscureciendo el mapa y tapando el propio Popup HUD. Con la refactorización, el clic sobre el pin únicamente enfoca el predio en 2D y despliega el Popup HUD de 360px manteniendo el mapa iluminado; la apertura profunda del Sheet lateral se reserva exclusivamente para la pulsación de "Abrir Inspección Técnica" dentro del Popup o los botones "Ficha" / "Matching" del listado lateral `TerrenosSplitList`. Validación completa con 0 errores TypeScript (`npx tsc --noEmit`), 0 advertencias en ESLint (`npm run lint`), 18/18 PASS en integridad geoespacial y confirmación HTTP 200 OK en `/mapa`.
* **[ACC-063] [2026-09-05 18:55] Micro-Calibración Cartográfica Final, Corrección de Regex en Script de Integridad y Bloqueo Total Multi-Input de Pitch:** Auditoría forense independiente y verificación exhaustiva de coordenadas contra OpenStreetMap y Nominatim. 1) **Alineación de TR-SBOR-071 (Av. San Borja Sur 890):** Confirmada y blindada la ubicación exacta sobre el eje de Av. San Borja Sur cuadra 8 (`-12.1008800, -76.9928500`), a 2 cuadras del Pentagonito y Av. Boulevard (corrigiendo la ubicación errónea que aparecía al sur en la captura del usuario). 2) **Corrección de Terrenos Adicionales en terrenos-seed.ts:** Detección y corrección de discrepancias en `TR-MIRA-084` (reubicado a cuadra 6 de Balboa en el número 615/640: `-12.1306000, -77.0251000`), `TR-LMOL-039` (reubicado de cuadra 11 a cuadra 5 frente al Campus USIL: `-12.0728000, -76.9538000`) y `TR-ATE-090` (ajustado a la acera sur de Av. Nicolás Ayllón en el distrito de Ate: `-12.0592000, -76.9742000`). 3) **Bloqueo Total de Perspectiva 3D y Pitch en terrenos-map.tsx:** Incorporación de `minPitch: 0` en constructor de MapLibre, deshabilitación de controles por teclado `map.keyboard.disableRotation()`, adición de `pitch: 0` y `bearing: 0` en todos los `fitBounds`, y listener de evento defensivo `map.on("pitch", ...)` forzando 0° ante cualquier intento de inclinación. 4) **Corrección de Regex en Script de Integridad:** Corrección del regex en `scripts/verify-spatial-integrity.mjs` para capturar estrictamente IDs `tr-XX` (eliminando colisión con IDs de documentos `doc-XX`), incorporando además verificaciones de cuadrantes para Balboa, La Fontana (USIL) y Nicolás Ayllón (Ate). Verificación con 18/18 PASS (100%), `npm run lint` (0 errores) y `npx tsc --noEmit` (0 errores).
* **[ACC-062] [2026-09-05 18:40] Calibración Cartográfica 100% Vial de 18 Terrenos y Bloqueo Total de Perspectiva 3D/Pitch Cenital en MapLibre GL JS:** Investigación geográfica profunda con OpenStreetMap y geocodificación inversa/directa para alinear con exactitud milimétrica los 18 lotes de `src/lib/mock/terrenos-seed.ts`. 1) **Alineación de TR-SBOR-071 (Av. San Borja Sur 890):** Corregido el desplazamiento erróneo de 400 metros al sur (que caía en Calle Pablo Usandizaga / Jr. Maestro Chueca en la captura del usuario) reubicándolo exactamente sobre el eje de la Av. San Borja Sur cuadra 8 (`-12.1008800, -76.9928500`), a 2 cuadras del Pentagonito y Av. Boulevard. 2) **Auditoría Vial Integral de los 18 Terrenos:** Recalibración completa de todos los predios verificados con reverse geocoding en OpenStreetMap alcanzando 18/18 aciertos directos en su respectiva vía (incluyendo `TR-MIRA-084` en Av. Balboa, `TR-JMA-033` en Av. San Felipe frente a UP y Canal 2, `TR-LCE-014` en Av. Arequipa 2450 frente a Parque Castilla, `TR-MAG-058` en Jr. Salaverry 410, `TR-SMIG-092` en Costanera 1100, `TR-BCO-045` en Av. Pedro de Osma 180, `TR-LMOL-039` en Av. La Fontana 520, etc.). 3) **Bloqueo Inmutable de Pitch 3D:** En `src/components/mapa/terrenos-map.tsx`, eliminación de la inclinación oblicua (`pitch: 35` en `flyTo` al seleccionar predio) estableciendo `pitch: 0` cenital estricto, configuración en constructor MapLibre de `pitch: 0`, `maxPitch: 0`, `dragRotate: false`, `touchPitch: false`, `pitchWithRotate: false` y deshabilitación explícita de `dragRotate`, `touchPitch` y rotación en `touchZoomRotate`. 4) **Validación y Suite:** Enriquecimiento de `scripts/verify-spatial-integrity.mjs` con verificación de corredores viales exactos y costa de San Miguel (18/18 PASS 100%). Verificación con `npm run lint` (0 errores) y `npx tsc --noEmit` (0 errores).
* **[ACC-061] [2026-09-05 18:20] Alineación Vial de TR-SISI-019 (Calle Las Palmeras), Rediseño de Popup HUD a 360px y Creación de Validador Geoespacial:** Investigación profunda mediante subagentes especializados (Especialista Cartográfico y Diseñador UI/UX) utilizando `systematic-debugging` y `uxui-principles`. 1) **Alineación Vial:** Corrección del predio `tr-002` (Calle Las Palmeras 380, San Isidro, `TR-SISI-019`), reubicado desde la vía privada "Vía Real" del Centro Empresarial Real a su eje oficial en Calle Las Palmeras cdra 3 (`-12.0944500, -77.0386500`), junto con micro-calibraciones viales en `tr-001` (Balboa), `tr-008` (Arequipa), `tr-010` (Principal), `tr-012` (2 de Mayo), `tr-013` (Encalada) y `tr-014` (Rivera Navarrete). 2) **Rediseño del Popup HUD:** Detección de la restricción oculta de MapLibre GL JS (`max-width: 240px` por defecto). Expansión del popup a 360px (`maxWidth: "360px"` en constructor y en `globals.css`), creación de zona de exclusión (`pr-8`, 32px) para aislar el botón `[X]` sin superposición sobre el badge `CZ` ni el estado comercial, erradicación de `truncate` rígido en dirección y referencia permitiendo lectura multilínea completa (`line-clamp-2`, `break-words`). 3) **Blindaje Geoespacial Futuro:** Creación del módulo de validación catastral `src/lib/map/geo-validator.ts` con Bounding Boxes oficiales para 15 distritos de Lima y detección de puntos marítimos, y suite automatizada `scripts/verify-spatial-integrity.mjs` con 18/18 lotes aprobados al 100%. Validación con `npx tsc --noEmit` (0 errores), `npm run lint` (0 advertencias) y HTTP 200 en `/mapa`.
* **[ACC-060] [2026-09-05 17:42] Optimización de Rendimiento a 60fps en MapLibre GL JS y Recalibración Cartográfica WGS84 de Terrenos:** Investigación previa con subagentes especializados (`systematic-debugging`, `phase-gated-debugging`, GIS/Real Estate y Arquitectura Frontend). Identificación y corrección de dos problemas críticos en el Módulo B: 1) **Lag severo al arrastrar:** En `src/components/mapa/terrenos-map.tsx`, el nodo DOM raíz del marcador (`el`) incluía `transition-all duration-200 / duration-150`, interceptando las escrituras inline de `style.transform` enviadas por MapLibre a 60 fps y retrasando los pines de manera continua hasta que el usuario soltaba el mouse. Implementación del patrón de Aislamiento DOM de Marcadores: el nodo raíz `el` permanece limpio (`cursor-pointer select-none`), mientras que las transiciones de escala (`scale-105`, `scale-110`, `hover:scale-105`) se delegaron a un contenedor hijo interno `.pin-container`, alcanzando paneo instantáneo a 60 fps sin retardo. 2) **Terrenos en el océano y acantilados:** En `src/lib/mock/terrenos-seed.ts`, recalibración completa de coordenadas WGS84 (`latitud`, `longitud` y objeto `geom`) para situar todos los lotes en tierra firme: `tr-005` (San Miguel, Costanera 1100) retirado 1.4 km del mar a su manzana urbana (`-12.0848, -77.0879`), `tr-007` (Magdalena del Mar, Jr. Salaverry 410) retirado del acantilado a su ubicación real (`-12.0912, -77.0694`), `tr-015` (Pueblo Libre) alineado a la Av. Sucre (`-12.0772, -77.0645`), `tr-008` (Lince) corregido en su límite distrital (`-12.0833, -77.0340`), `tr-003` (Surco) subido 1.3 km al eje El Derby (`-12.1008, -76.9742`) y saneada la referencia de `tr-012` (Miraflores). Verificación de compilación TypeScript con 0 errores (`npx tsc --noEmit`), linter aprobado con 0 warnings (`npm run lint`) y respuesta HTTP 200 en `/mapa`.

## 📅 2026-09-04 (Planificación Estratégica & Registro de Pendientes Fase 2)

### 📌 Hoja de Ruta y Próximos Hitos del Sistema
* **[ACC-059] [2026-09-04 18:43] Desacoplamiento de Ciclo de Vida en MapLibre GL JS, Retención Estricta de Viewport y Estandarización Light Theme de Marcadores:** Investigación profunda previa mediante subagentes especializados (`systematic-debugging` y `uxui-principles`) para erradicar el reseteo involuntario de zoom (zoom out a Lima metropolitana) reportado por el usuario. Identificación de causa raíz en `src/components/mapa/terrenos-map.tsx`: acoplamiento de `hoveredTerrenoId` en las dependencias del `useEffect` de marcadores y ejecución incondicional de `map.fitBounds(bounds)` ante cualquier hover/unhover en pines o tarjetas de `TerrenosSplitList`. Refactorización arquitectónica en 3 efectos ortogonales independientes: 1) Creación y reconciliación de marcadores (`[terrenos, isMapLoaded]`) desacoplada de callbacks mediante refs (`onSelectTerrenoRef`, `onHoverTerrenoRef`, `selectedTerrenoIdRef`); 2) Actualización visual atómica de hover y selección (`[selectedTerrenoId, hoveredTerrenoId]`) manipulando estilos directos sobre el DOM en GPU (`scale`, `z-index`, bordes y sombras) con cero afectación de cámara y cero recreación de marcadores; 3) Control de cámara inteligente (`[terrenos, isMapLoaded]`) con banderas `hasInitialFittedRef` y `prevTerrenosKeyRef` que solo dispara `fitBounds` en la carga inicial o ante un cambio real en los filtros aplicados y sin lote seleccionado. Sincronización en `src/app/mapa/mapa-client.tsx` con `useCallback` para estabilizar referencias de `handleSelectTerreno` y `handleHoverTerreno`. Estandarización de pines a Bloomberg Light Theme estricto (fondo blanco `#ffffff`, borde pizarra `#cbd5e1`, tipografía oscura `$3,250/m²` y badges de zonificación pastel RDA/CZ/CM/RDM, eliminando fondo oscuro `#0f172a`). Verificación exitosa con `npx tsc --noEmit` (0 errores), `npm run lint` (0 advertencias) y HTTP 200 OK en `/mapa`.
* **[ACC-058] [2026-09-04 14:46] Depuración Integral de Enlaces Inter-Módulos, Sincronización Pipeline/Comisiones/Auditoría y Verificación E2E 100%:** Corrección en `src/lib/services/negociaciones.ts` para permitir búsqueda libre por ID de deal (`neg-001`), ID de terreno (`tr-001`, `terr-001`), ID normalizado y propietario. Sincronización en `src/app/pipeline/pipeline-client.tsx` del query parameter `terrenoId` con el estado `busqueda` de la barra de herramientas. Sincronización en `src/app/matching/matching-client.tsx` para filtrar por `terreno.id` y `normQ` en vista cliente. Enlace con dealId en `src/components/comisiones/comision-detail-sheet.tsx` (`/pipeline?dealId=...`). Sobrescritura directa de `busqueda` en `/mapa` y `/terrenos` ante nuevos query parameters. Normalización de ID en `src/app/documentos/documentos-client.tsx`. Soporte de `useSearchParams` con `<Suspense>` en `/auditoria` (`q`, `entidadId`, `modulo`) y `/reportes` (`brokerId`, `periodo`). Cierre selectivo de dropdowns (`closeOnSelect={true}`) en filtros single-select de brokers. Suite E2E con Chrome CDP (`scripts/verify-comprehensive-e2e.mjs`) aprobada con 10/10 tests (100% PASS, exit code 0) y validación completa con `npm run lint` y `npx tsc --noEmit` (0 errores).
* **[ACC-057] [2026-09-04 14:25] Resolución de Error Crítico de React Hooks en QuickDealDialog, Sincronización de Búsqueda en Mapa y Depuración Integral de Enlaces Inter-Módulos:** Reubicación incondicional de todos los `useState` al nivel superior en `src/components/matching/quick-deal-dialog.tsx` antes de `if (!match) return null;` con sincronización en `useEffect([match])`, erradicando la violación a `rules-of-hooks`. Sincronización en `src/app/mapa/mapa-client.tsx` de búsqueda por ID con normalización `normQ` (`terr-` a `tr-`) y zonificación en `filteredTerrenos`. Corrección del hook warning de dependencias en `src/components/mapa/terrenos-map.tsx` inicializando con `MAP_STYLES.positron`. Soporte y normalización universal de parámetros `codigo`, `terrenoId`, `q` y `tab` en `/terrenos`, `/mapa`, `/documentos`, `/pipeline`, `/comisiones` y `/matching`. Verificación exhaustiva con 0 errores y 0 warnings en `npm run lint` y `npx tsc --noEmit` (código 0) y comprobación HTTP 200 OK en todas las rutas.
* **[ACC-056] [2026-09-04 14:15] Auditoría Forense y Corrección E2E de Checkboxes, Navegación Inter-Módulos y Cumplimiento Light Theme Bloomberg:** Identificación y corrección de cierre involuntario en `DropdownMenuCheckboxItem` (`src/components/ui/dropdown-menu.tsx`) mediante `event.preventDefault()` en `onSelect`. Normalización de query params case-insensitive en `/documentos` y corrección de enlaces en `page.tsx` (`/documentos?estado=por_vencer`), resolviendo la tabla vacía y mostrando los 6 documentos por vencer. Búsqueda y resolución de IDs entre seeds (`terr-` vs `tr-`) en `/terrenos`, `/mapa`, `/matching` y `/pipeline`. Adición de enlaces faltantes en `match-by-cliente.tsx`. Erradicación total de clases oscuras residuales (`bg-slate-900`, `bg-slate-950`) en `button.tsx`, `badge.tsx`, `dialog.tsx`, `sheet.tsx`, `gis-db-monitor.tsx`, `terreno-detail-sheet.tsx`, `deal-detail-sheet.tsx`, `configuracion-client.tsx`, `pdf-viewer-frame.tsx` y `vigencia-badge.tsx`. Validación E2E real con Chrome CDP (6 suites aprobadas al 100%) y `npx tsc --noEmit` con 0 errores.
* **[ACC-055] [2026-09-04 13:54] Modelado Aéreo de Edificio de Muestra (16p / 9p) en Lote Derecho:** Generación y renderizado de la torre de muestra en el predio derecho sobre la huella arquitectónica suministrada (16 pisos frente a Av. Guardia Civil, 9 pisos frente a Jr. Los Próceres, ala lateral de conexión y patio central ajardinado correspondiente a la zona roja). Generación de vistas panorámicas duales (con rotulación de calles y limpia de alta definición) integrando el proyecto Pionero 314 a la izquierda y el edificio de muestra a la derecha. Actualización y sincronización de entregables e informes (MD y HTML) en el escritorio del usuario en `Estudio_Guardia_Civil_314_Pionero`.
* **[ACC-054] [2026-09-04 13:52] Corrección Integral de Checkboxes Radix, Doble-Clic y Navegación Reactiva Entre Módulos:** Eliminación del bug de doble-toggle en Checkboxes causado por envolturas `<label>` sobre botones primitivos de Radix UI en filtros de CPU y estados de terrenos (`terrenos-filters.tsx`), selector de columnas (`terrenos-toolbar.tsx`) y celdas de TanStack Table v8 (`terrenos-columns.tsx`, `terrenos-table.tsx`, `documentos-columns.tsx`, `documentos-table.tsx`, `comisiones-columns.tsx`, `comisiones-table.tsx`), aislando los clics con `e.stopPropagation()` para evitar la apertura indeseada del sheet de detalle. Implementación de interoperabilidad y navegación reactiva completa con `useSearchParams` encapsulado en `<Suspense>` en `/terrenos`, `/documentos`, `/pipeline`, `/comisiones`, `/matching` y `/mapa`. Adición de enlaces bidireccionales `Link` en: Header corporativo (búsqueda global con Enter, badges de inventario y valor hacia `/terrenos` y `/reportes`), Dashboard principal (KPIs, filas de terrenos hacia `/terrenos?q=...`, `/documentos?q=...` y `/matching?terrenoId=...`), Drawer de terreno (`TerrenoDetailSheet` hacia `/mapa`, `/pipeline`, `/documentos`), Drawer de negociación (`DealDetailSheet` hacia ficha de terreno, mapa y comisiones), League Table de brokers en `/reportes` (hacia `/pipeline` y `/comisiones`), Auditoría Forense (`auditoria-columns.tsx` hacia el módulo de cada entidad afectada) y creación rápida de deals en Matching hacia `/pipeline?dealId=...`. Validación de compilación TypeScript estricta con 0 errores (`npx tsc --noEmit`) y comprobación de código HTTP 200 en las 10 rutas activas del sistema.
* **[ACC-053] [2026-09-04 13:35] Investigación Urbanística y Modelado Aéreo de Av. Guardia Civil 314 (Pionero):** Investigación exhaustiva de normativa de alturas y zonificación (CZ/RDA vs RDM/VIS, Ordenanzas 1076 y 2504-2022-MML) en Chorrillos mediante subagente de investigación. Generación de visualizaciones aéreas hiperrealistas con el edificio "Guardia Civil Sur 314" de 15 niveles de Inmobiliaria Pionero en el lote izquierdo, acondicionamiento del lote derecho como terreno vacío limpio y elevación del entorno a alturas consolidadas realistas (8-12 pisos en avenida y 4-5 pisos interiores). Organización y entrega completa de archivos (renders limpios y rotulados, referencias oficiales e informes técnicos MD y HTML) en el escritorio del usuario en `Estudio_Guardia_Civil_314_Pionero`.
* **[ACC-052] [2026-09-04 12:28] Inicialización y Verificación del Servidor de Desarrollo:** Levantamiento en segundo plano del servidor Next.js 14.2.35 en `http://localhost:3000` en cumplimiento con la Regla 1 de `AGENTS.md`. Verificación de conectividad mediante `Invoke-WebRequest` recibiendo código de estado HTTP 200 OK y compilación inicial completada.
* **[ACC-051] [2026-09-04 02:07] Actualización de la Hoja de Ruta y Registro de Pendientes (Fase 2):** Incorporación formal en `MEMORY.md` de las 5 iniciativas estratégicas de la Fase 2: Unificación Reactiva del Dashboard Principal & Command Palette Global (`Ctrl+K`), Módulo I de Constructoras y Mandatos de Inversión (`/constructoras`), Formulario Integral de Alta de Terrenos con Previsualización GIS y carga de CPU, Generador de Ficha Comercial & Dossier Ejecutivo de Inversión en PDF, y Persistencia en Base de Datos Real (Supabase / PostgreSQL + PostGIS).

## 📅 2026-09-03 (Módulo G: Métricas, Rendimiento BI & Reportes Ejecutivos)

### 📈 Business Intelligence, Métricas Ejecutivas & Reportes para Comités
* **[ACC-043] [2026-09-03 00:30] Investigación y Plan de Implementación Módulo G:** Ejecución de subagentes especializados de investigación (Negocio/UX y Arquitectura Técnica) y elaboración del Plan de Implementación formal para el Módulo G: Métricas, Rendimiento BI & Reportes Ejecutivos, con análisis de volumen transaccionado en suelo, Pipeline Velocity, Win Rate, League Table de brokers, matriz de absorción territorial de suelo, gráficos en SVG nativo + Tailwind y modal de informe ejecutivo imprimible para comités de inversión.
* **[ACC-044] [2026-09-03 00:35] Implementación Completa Módulo G (Métricas, Rendimiento BI & Reportes Ejecutivos):** Creación del tipado estricto (`src/types/reportes.ts`), exportación en `src/types/index.ts`, motor de agregación analítica reactiva multi-módulo (`src/lib/services/reportes.ts`), exportador a Excel CSV multi-sección (`export-reportes.ts`), componentes UI Bloomberg Light Theme (`reportes-kpi-banner.tsx`, `reportes-toolbar.tsx`, `evolucion-financiera-chart.tsx`, `embudo-conversion-chart.tsx`, `ranking-brokers-table.tsx`, `absorcion-mercado-grid.tsx`, `reporte-ejecutivo-modal.tsx`), ruta Server y Client `/reportes` (`page.tsx`, `reportes-client.tsx`), actualización de `sidebar.tsx` con badge "Mod G", verificación de TypeScript con 0 errores (`npx tsc --noEmit`) y prueba HTTP 200 OK.

### 🛡️ Gobernanza Normativa, Calibración Algorítmica & Auditoría Forense Global
* **[ACC-045] [2026-09-03 01:25] Investigación y Plan de Implementación Módulo H (Configuración GIS, Parámetros Normativos & Auditoría Global):** Ejecución de subagentes especializados de investigación (Negocio/UX y Arquitectura Técnica) y diseño del plan técnico para la división en dos submódulos corporativos: Submódulo H1 (`/configuracion`) para gobernanza de 22 ordenanzas distritales de Lima, calibrador algorítmico del motor de matching con validación estricta de suma 100% y monitor de telemetría PostGIS EPSG:4326; y Submódulo H2 (`/auditoria`) para la bitácora forense append-only inmutable de transacciones, diff engine campo por campo (Antes vs. Después) y exportación compatible con Excel en español.
* **[ACC-046] [2026-09-03 01:35] Implementación Completa Módulo H (Configuración GIS, Parámetros Normativos & Auditoría Global):** Creación del tipado estricto (`src/types/configuracion.ts`, `src/types/auditoria.ts`), exportación en `src/types/index.ts`, dataset seed de 22 normas urbanísticas de Lima (`parametros-normativos-seed.ts`), dataset seed de 25 eventos históricos forenses (`auditoria-seed.ts`), capa de servicios reactiva con pub/sub y cálculo automático de diffs (`src/lib/services/configuracion.ts`, `src/lib/services/auditoria.ts`), exportador forense a CSV con BOM UTF-8 (`export-auditoria.ts`), componentes UI en Bloomberg Light Theme (`configuracion-tabs.tsx`, `parametro-edit-dialog.tsx`, `parametros-urbanisticos-table.tsx`, `matching-config-panel.tsx`, `gis-db-monitor.tsx`, `auditoria-kpi-banner.tsx`, `auditoria-toolbar.tsx`, `auditoria-columns.tsx`, `auditoria-table.tsx`, `auditoria-diff-sheet.tsx`), rutas Server y Client `/configuracion` y `/auditoria` (`page.tsx`, `configuracion-client.tsx`, `auditoria-client.tsx`), actualización del sidebar con badges "Mod H1" y "Mod H2", verificación estricta de TypeScript con 0 errores (`npx tsc --noEmit`) y verificación HTTP 200 OK en ambos endpoints.

### 🗺️ Visor Cartográfico: Rediseño Light Theme Corporativo del Map Popup HUD & Controles
* **[ACC-047] [2026-09-03 01:40] Investigación y Plan de Rediseño Light Theme para Map Popup HUD y Controles MapLibre:** Ejecución de subagentes especializados de investigación (UX/Negocio Inmobiliario y Arquitectura Técnica MapLibre) utilizando las skills `design-taste-frontend` y `uxui-principles` para resolver la infracción de fondos oscuros (`AGENTS.md`) y la colisión física entre el botón de cierre `[X]` y el badge de zonificación en el visor cartográfico.
* **[ACC-048] [2026-09-03 01:43] Implementación Rediseño Light Theme del Map Popup HUD y Controles MapLibre:** Actualización de `src/app/globals.css` (estilos `.promundo-map-popup` claros, tip en `#ffffff` para todas las anclas y botón de cierre delimitado `22x22px`), `src/components/mapa/terrenos-map.tsx` (zona de exclusión `pr-7` para evitar solapamiento con el botón de cierre, badges pastel para estado y zonificación, métricas Bloomberg legibles, indicador CPU vectorial sin emojis, contenedor `bg-slate-100`, selector de capa base y controles flotantes `bg-white/95 border-slate-200`) y `src/components/mapa/map-skeleton.tsx` (eliminación de parpadeo oscuro a `bg-slate-100`). Validación exitosa con `npx tsc --noEmit` (0 errores) y HTTP 200 OK en `http://localhost:3000/mapa`.

### 🧭 Navegación Global: Sidebar Retráctil Corporativo (Icon-Only & Radix Tooltips)
* **[ACC-049] [2026-09-03 01:46] Investigación de Arquitectura y UX para Sidebar Retráctil:** Ejecución de subagentes especializados de investigación (UX/Enterprise Design y Arquitectura React/Next.js) utilizando las skills `design-taste-frontend` y `minimalist-ui` para diseñar el modo colapsado (`w-14` / 56px), tooltips flotantes Radix, prevención de hydration mismatch en App Router y emisión de resize events al terminar la transición.
* **[ACC-050] [2026-09-03 01:49] Implementación de Sidebar Retráctil Corporativo (Bloomberg Light Theme):** Refactorización completa de `src/components/layout/sidebar.tsx` incorporando modo icon-only (`w-14`), persistencia en `localStorage` (`promundo:sidebar-collapsed`), atajo de teclado `Ctrl+B`, botones de toggle `PanelLeftClose`/`PanelLeftOpen`, Tooltips Radix enriquecidos a la derecha (`side="right"`), separadores sutiles de categoría, erradicación de fondos oscuros a estándar `bg-white border-slate-200` y actualización de `src/components/ui/tooltip.tsx` a Light Theme. Verificación con 0 errores TypeScript (`npx tsc --noEmit`) y respuestas HTTP 200 OK en todas las rutas.

---

## 📅 2026-09-02 (Módulo B: Mapa Interactivo Georreferenciado & WebGL)

### 🗺️ Cartografía Geoespacial & Componentes MapLibre
* **[ACC-023] [2026-09-02 00:21] Investigación y Plan de Implementación Módulo B:** Ejecución de subagentes especializados de investigación (Negocio/UX y Arquitectura Cartográfica) y elaboración del Plan de Implementación formal para el Mapa Interactivo Georreferenciado con MapLibre GL JS, capas conmutables y vista dividida.
* **[ACC-024] [2026-09-02 02:14] Instalación de MapLibre GL:** Instalación de `maplibre-gl@^4.7.1` incorporando tipado oficial de TypeScript y soporte WebGL.
* **[ACC-025] [2026-09-02 02:15] Configuración de Capas y Estilos Cartográficos:** Creación de `src/lib/map/map-styles.ts` con definiciones de capas vectoriales (CartoDB Positron / Voyager) y satelitales de alta resolución (Esri World Imagery + Reference Labels).
* **[ACC-026] [2026-09-02 02:15] Esqueleto de Carga MapSkeleton:** Creación de `src/components/mapa/map-skeleton.tsx` con grid y estado de carga de alta densidad.
* **[ACC-027] [2026-09-02 02:15] Componente WebGL TerrenosMap:** Creación de `src/components/mapa/terrenos-map.tsx` con ciclo de vida `useRef`, observador `ResizeObserver`, renderizado de marcadores HTML personalizados con zonificación y precio/$m², centrado automático `fitBounds`/`flyTo`, micro-tarjetas Popup interactivas y controles de navegación.
* **[ACC-028] [2026-09-02 02:15] Wrapper Dinámico SSR MapaDynamic:** Creación de `src/components/mapa/mapa-dynamic.tsx` encapsulado con `next/dynamic` y `{ ssr: false }`.
* **[ACC-029] [2026-09-02 02:15] Panel Lateral Split List:** Creación de `src/components/mapa/terrenos-split-list.tsx` para la vista dividida 60/40, con tarjetas de alta densidad, ordenamiento dinámico por $/m², área y altura, y botones de acceso directo a Ficha y Matching.
* **[ACC-030] [2026-09-02 02:15] Barra de Herramientas MapaToolbar:** Creación de `src/components/mapa/mapa-toolbar.tsx` con switch de modo Split 60/40 vs 100% Pantalla Completa, contador de lotes filtrados y link al Data Grid.
* **[ACC-031] [2026-09-02 02:16] Orquestador Cliente y Página /mapa:** Creación de `src/app/mapa/mapa-client.tsx` y `src/app/mapa/page.tsx` con integración reactiva de filtros, mapa, lista y Drawer lateral de 5 pestañas (`TerrenoDetailSheet`).
* **[ACC-032] [2026-09-02 02:16] Estilos Globales para Popups:** Inclusión en `src/app/globals.css` de estilos oscuros de alta densidad para popups de MapLibre GL.
* **[ACC-033] [2026-09-02 02:17] Validación y Compilación Exitosa:** Verificación de 0 errores TypeScript (`npx tsc --noEmit`) y comprobación de respuesta HTTP 200 OK en `http://localhost:3000/mapa`.
* **[ACC-034] [2026-09-02 02:24] Investigación y Plan de Implementación Módulo C:** Ejecución de subagentes especializados de investigación (Negocio/UX y Arquitectura Técnica) y elaboración del Plan de Implementación formal para la Gestión Documental & Certificados de Parámetros Urbanísticos (CPU), con semáforo reactivo de vigencias, visor de PDF integrado, carga Drag & Drop y Data Grid TanStack Table v8.
* **[ACC-035] [2026-09-02 02:37] Implementación Completa Módulo C (Gestión Documental & CPU):** Creación del tipado estricto (`src/types/documentos.ts`), dataset seed (`documentos-seed.ts`), capa de servicios reactiva (`src/lib/services/documentos.ts`) con semáforos de vigencia (`vigencia-badge.tsx`), visor de PDF nativo (`pdf-viewer-frame.tsx`) y modal expandido (`pdf-viewer-modal.tsx`), banner de 5 KPIs (`documentos-kpi-banner.tsx`), barra de filtros (`documentos-toolbar.tsx`), Data Grid TanStack Table v8 (`documentos-table.tsx`, `documentos-columns.tsx`), modal de carga Drag & Drop (`documento-upload-dialog.tsx`) con cálculo +36m Ley 29090, integración completa con el Drawer lateral (`TerrenoDetailSheet`) y ruta `/documentos` verificada con 0 errores TypeScript y HTTP 200 OK.
* **[ACC-036] [2026-09-02 10:35] Investigación y Plan de Implementación Módulo D:** Ejecución de subagentes especializados de investigación (Negocio/UX y Arquitectura Técnica) y elaboración del Plan de Implementación formal para el Pipeline de Negociaciones & Bitácora de Auditoría, con 7 etapas del embudo comercial, modal obligatorio de transición, bitácora inmutable y exportación a Excel.
* **[ACC-037] [2026-09-02 10:41] Implementación Completa Módulo D (Pipeline de Negociaciones & Bitácora de Auditoría):** Creación del tipado estricto (`src/types/negociaciones.ts`), dataset seed (`negociaciones-seed.ts`), capa de servicios reactiva (`src/lib/services/negociaciones.ts`) con cálculo de valor nominal, ponderado ($ USD), comisiones al 3% y alertas de SLA (>14d), componentes UI (`stage-badge.tsx`, `pipeline-kpis.tsx`, `pipeline-toolbar.tsx`, `deal-card.tsx`, `kanban-column.tsx`, `pipeline-kanban.tsx`, `etapa-change-dialog.tsx`, `deal-detail-sheet.tsx`, `deal-create-dialog.tsx`, `pipeline-table-view.tsx`), exportador a Excel CSV (`export-pipeline.ts`), sincronización con `TerrenoDetailSheet`, ruta `/pipeline` verificada con 0 errores TypeScript (`npx tsc --noEmit`) y HTTP 200 OK.
* **[ACC-038] [2026-09-02 13:12] Investigación y Plan de Implementación Módulo E:** Ejecución de subagentes especializados de investigación (Negocio/UX y Arquitectura Técnica) y elaboración del Plan de Implementación formal para el Motor de Matching Automático Lote ↔ Constructoras, con algoritmo bidireccional de 5 factores cuantitativos, tolerancias de mercado en Lima, matriz de calor interactiva NxM, calibrador de ponderaciones e integración comercial con el Módulo D de Negociaciones.
* **[ACC-039] [2026-09-02 13:24] Implementación Completa Módulo E (Motor de Matching Automático Lote ↔ Constructoras):** Creación del tipado estricto (`src/types/matching.ts`), ampliación de la capa de servicios reactiva y memoizada (`src/lib/services/matching.ts`) con 5 criterios inmobiliarios, clústeres de distritos de Lima, cálculo cuantitativo de gaps y matriz NxM en memoria, banner de 5 KPIs (`matching-kpis.tsx`), barra de herramientas (`matching-toolbar.tsx`), calibrador de ponderaciones (`matching-weights-popover.tsx`), vistas operativas (`match-by-terreno.tsx`, `match-by-cliente.tsx`, `matching-matrix.tsx`), modal rápido a pipeline (`quick-deal-dialog.tsx`), drawer de auditoría (`match-detail-drawer.tsx`), exportador a Excel CSV (`export-matching.ts`), sincronización con `TerrenoDetailSheet`, ruta `/matching` verificada con 0 errores TypeScript (`npx tsc --noEmit`) y HTTP 200 OK.
* **[ACC-040] [2026-09-02 13:46] Erradicación Total de Fondos Oscuros en Todo el Sistema (Estandarización Light Theme Corporativo):** Modificación de las directrices en `AGENTS.md` con prohibición estricta de fondos oscuros. Refactorización estética integral a estándar Bloomberg / FactSet de alta densidad con fondos claros (`bg-white`, `bg-slate-50`, `bg-slate-100`), bordes sutiles `border-slate-200` y tipografías oscuras `text-slate-800/900` en: Dashboard (`page.tsx`), Módulo Documentos y CPU (`documentos-client.tsx`, `documentos-kpi-banner.tsx`, `documentos-toolbar.tsx`, `documentos-table.tsx`, `documentos-columns.tsx`, `pdf-viewer-frame.tsx`, `pdf-viewer-modal.tsx`, `documento-upload-dialog.tsx`), Módulo Pipeline (`pipeline-toolbar.tsx`, `kanban-column.tsx`, `pipeline-table-view.tsx`, `deal-detail-sheet.tsx`, `deal-create-dialog.tsx`, `etapa-change-dialog.tsx`), Módulo Matching Constructoras (`matching-kpis.tsx`, `matching-toolbar.tsx`, `match-by-terreno.tsx`, `match-by-cliente.tsx`, `matching-matrix.tsx`, `matching-weights-popover.tsx`, `match-detail-drawer.tsx`, `quick-deal-dialog.tsx`), Módulo Mapa (`mapa-client.tsx`, `mapa-toolbar.tsx`, `terrenos-split-list.tsx`), Ficha de Terreno (`terreno-detail-sheet.tsx`) y Primitivos UI (`dialog.tsx`). Verificación exitosa de TypeScript con 0 errores (`npx tsc --noEmit`).
* **[ACC-041] [2026-09-02 22:52] Investigación y Plan de Implementación Módulo F:** Ejecución de subagentes especializados de investigación (Negocio/UX y Arquitectura Técnica) y elaboración del Plan de Implementación formal para el Módulo F: Comisiones, Liquidaciones y Reportes Financieros, con desglose normativo peruano (IGV 18%, SPOT 12% SUNAT en Banco de la Nación, IRPF 4ta categoría 8% con excepción por Formulario 1609), splits configurables de corretaje (50/50 o 25/25/50), Data Grid TanStack Table v8, Sheet lateral de liquidación tributaria, modal de transición de estado y exportación a Excel CSV.
* **[ACC-042] [2026-09-02 23:02] Implementación Completa Módulo F (Comisiones, Liquidaciones y Reportes Financieros):** Creación del tipado estricto (`src/types/comisiones.ts`), dataset seed enriquecido de 7 liquidaciones (`comisiones-seed.ts`), capa de servicios reactiva (`src/lib/services/comisiones.ts`) con aritmética financiera segura a 2 decimales y cálculo de impuestos SPOT/IGV, exportador a Excel CSV con balance consolidado (`export-comisiones.ts`), componentes UI en Bloomberg Light Theme (`comisiones-kpi-banner.tsx`, `comisiones-toolbar.tsx`, `comisiones-columns.tsx`, `comisiones-table.tsx`, `comision-detail-sheet.tsx`, `estado-liquidacion-dialog.tsx`, `comision-voucher-modal.tsx`), ruta Server y Client `/comisiones` (`page.tsx`, `comisiones-client.tsx`), actualización del sidebar con badge "Mod F", verificación exitosa de TypeScript con 0 errores (`npx tsc --noEmit`) y prueba HTTP 200 OK.


---

## 📅 2026-09-01 (Inicialización, Arquitectura PostGIS & Protocolo de Agentes)

### 🏗️ Arquitectura, Base de Datos y Entorno
* **[ACC-001] [2026-09-01 14:07] Inicialización de Configuración Base:** Creación de `package.json`, `tsconfig.json`, `tailwind.config.ts`, `globals.css`, `drizzle.config.ts`, `components.json`, `.gitignore` y `.env.example` con soporte para tipado estricto y Tailwind CSS compacto (12-13px, paleta Slate/Bloomberg).
* **[ACC-002] [2026-09-01 14:08] Soporte Geoespacial PostGIS:** Creación de tipo personalizado `postgisGeometryPoint` en `src/db/schema/custom-types.ts` para columnas `geometry(Point, 4326)` con parsing WKT y GeoJSON.
* **[ACC-003] [2026-09-01 14:08] Enums de PostgreSQL:** Creación de `src/db/schema/enums.ts` con los 8 enums de dominio (`rol_usuario`, `tipo_documento`, `moneda`, `estado_terreno`, `tipo_cliente`, `etapa_negociacion`, `tipo_evento_bitacora`, `estado_pago_comision`).
* **[ACC-004] [2026-09-01 14:08] Esquema Relacional Drizzle ORM:** Creación de `src/db/schema/tables.ts` con las 8 tablas de negocio (`usuarios`, `propietarios`, `terrenos`, `documentos_terreno`, `clientes`, `negociaciones`, `bitacora_negociacion`, `comisiones_cierres`), índices B-Tree y llaves foráneas.
* **[ACC-005] [2026-09-01 14:08] Relaciones y Queries Anidadas:** Creación de `src/db/schema/relations.ts` y exportación consolidada en `src/db/schema/index.ts`.
* **[ACC-006] [2026-09-01 14:08] Conexión de Base de Datos:** Creación de cliente de base de datos Drizzle en `src/db/index.ts` optimizado para entornos serverless y pooling de conexiones.
* **[ACC-007] [2026-09-01 14:08] Script de Migración SQL PostGIS:** Creación de `src/db/migrations/0000_init_postgis_schema.sql` que incluye `CREATE EXTENSION postgis`, índices espaciales `GIST (geom)` y trigger de sincronización automática de coordenadas a geometría.
* **[ACC-008] [2026-09-01 14:08] Clientes de Supabase SSR & Admin:** Creación de `src/lib/supabase/client.ts` y `src/lib/supabase/server.ts` con soporte para Server Components y rol de administración.
* **[ACC-009] [2026-09-01 14:08] Tipos e Interfaces de Dominio:** Creación de `src/types/index.ts` con tipos inferidos de Drizzle (`InferSelectModel`), interfaces de filtros acumulativos `AND` y tipos de matching.
* **[ACC-010] [2026-09-01 14:08] Utilidades de Formato Inmobiliario:** Creación de `src/lib/utils.ts` con utilidades de combinación Tailwind (`clsx`, `twMerge`), formateadores monetarios USD/PEN y métricas de área ($m^2$, precio/$m^2$).

### 🖥️ Interfaz de Usuario y Layout
* **[ACC-011] [2026-09-01 14:08] Componentes UI de Alta Densidad:** Creación de `src/components/ui/table.tsx` (filas compactas de 32px), `badge.tsx`, `button.tsx` y `card.tsx`.
* **[ACC-012] [2026-09-01 14:09] Shell y Layout del Sistema:** Creación de barra lateral `Sidebar` (`src/components/layout/sidebar.tsx`), barra superior `Header` (`src/components/layout/header.tsx`) con ticker de métricas en tiempo real y buscador `Ctrl+K`.
* **[ACC-013] [2026-09-01 14:09] Dashboard de Control Inicial:** Creación de `src/app/page.tsx` con KPIs de volumen de inversión, inventario de lotes, alertas de vencimiento y monitor técnico de PostGIS.

### ⚙️ Entorno, Validación y Ejecución
* **[ACC-014] [2026-09-01 17:08] Instalación de Paquetes:** Ejecución de `npm install` incorporando 484 dependencias del ecosistema Next.js, Radix UI y Drizzle.
* **[ACC-015] [2026-09-01 17:19] Verificación Estricta TypeScript:** Corrección del tipado de cookies en `src/lib/supabase/server.ts` y validación exitosa con `npx tsc --noEmit` (0 errores).
* **[ACC-016] [2026-09-01 17:19] Puesta en Marcha del Servidor:** Levantamiento del servidor de desarrollo Next.js en segundo plano (`npm run dev`) operativo en `http://localhost:3000`.

### 📋 Protocolo de Agentes y Memoria
* **[ACC-017] [2026-09-01 17:25] Creación de Reglas AGENTS.md:** Redacción del documento formal de reglas operativas obligatorias para agentes y subagentes.
* **[ACC-018] [2026-09-01 17:25] Creación de Memoria Viva MEMORY.md:** Implementación del archivo de handoff con las últimas 10 acciones y la sección viva de pendientes por módulo.
* **[ACC-019] [2026-09-01 17:28] Creación de Archivo Histórico MEMORY_ARCHIVE.md:** Creación del registro acumulativo integral para la preservación permanente de todas las acciones del proyecto.
* **[ACC-020] [2026-09-01 17:29] Categorización Cronológica por Fechas:** Estructuración de `MEMORY_ARCHIVE.md`, `MEMORY.md` y `AGENTS.md` con agrupación estricta por fechas (`YYYY-MM-DD`).
* **[ACC-021] [2026-09-01 17:31] Investigación y Plan de Implementación Módulo A:** Ejecución de subagentes especializados de investigación (Negocio/UX y Arquitectura Técnica) y elaboración del Plan de Implementación formal para el Data Grid de Terrenos con TanStack Table v8, filtros acumulativos AND, Sheet lateral y exportación a Excel.
* **[ACC-022] [2026-09-01 20:00] Implementación Completa Módulo A (Data Grid de Terrenos):** Construcción de componentes UI primitivos compactos (`sheet.tsx`, `checkbox.tsx`, `popover.tsx`, `tabs.tsx`, `input.tsx`, `dropdown-menu.tsx`, `tooltip.tsx`, `separator.tsx`, `select.tsx`), dataset enriquecido con 18 lotes en Lima (`terrenos-seed.ts`), motor de matching algorítmico 0-100% (`matching.ts`), Data Grid con TanStack Table v8 (`terrenos-columns.tsx`, `terrenos-table.tsx`), filtros acumulativos AND (`terrenos-filters.tsx`), chips activos (`active-filter-chips.tsx`), Drawer lateral de 5 pestañas (`terreno-detail-sheet.tsx`), exportación Excel XLSX (`export-excel.ts`) y página `/terrenos` validada con 0 errores TypeScript.
