# MEMORY.md - Memoria Operativa & Handoff de "Promundo Sistema"

Este archivo mantiene el estado vivo del proyecto, el historial rotativo de las últimas 10 acciones ejecutadas (con fecha y hora) y el mapa de pendientes según lo estipulado en `AGENTS.md`.

---

### 🕒 ÚLTIMAS 10 ACCIONES EJECUTADAS (ROTATIVO)

### 📅 2026-09-06
1. **[2026-09-06 01:21] Implementación Integral del Alta de Nuevo Lote de Suelo Institucional (TerrenoCreateDialog) y Corrección de DialogTitle Light Theme:** Investigación previa mediante subagente especializado de dominio inmobiliario y arquitectura frontend (`clean-code`, `react-patterns`, `nextjs-app-router-patterns`). 1) **Detección de Causa Raíz:** El botón "+ Nuevo Lote" en `src/components/terrenos/terrenos-toolbar.tsx` se encontraba estático sin prop `onClick`, y la vista cliente `TerrenosClient` no contaba con diálogo ni estado para registrar activos. 2) **Corrección de DialogTitle en Tema Claro:** En `src/components/ui/dialog.tsx`, corrección de la clase residual `text-white` en `DialogTitle` reemplazándola por `text-slate-900` para garantizar legibilidad absoluta sobre el fondo corporativo blanco `bg-white` y cabecera `bg-slate-50`. 3) **Creación de TerrenoCreateDialog:** Desarrollo del componente `src/components/terrenos/terreno-create-dialog.tsx` con soporte para: autogeneración de código institucional de cartera (`TR-[DIST]-[SEQ]`) editable; alta rápida inline de propietarios titulares en SUNARP con DNI/RUC; selector de distritos metropolitanos de Lima con cálculo automático de coordenadas centroides referenciales; validación geoespacial en tiempo real con `validateTerrenoCoordinates` para prevenir coordenadas erróneas o marítimas en el Océano Pacífico; parámetros urbanísticos (zonificación RDA/RDM/CZ/CM/I1/ZTE, área m², frente lineal, fondo calculado, altura en pisos y usos permitidos); y cálculo bidireccional reactivo entre precio total (USD) y precio por metro cuadrado ($/m²). 4) **Integración de Servicios y Estado Reactivo:** Extensión de `src/lib/services/terrenos.ts` con `getPropietarios()`, `createPropietarioEnMemoria()` y `createTerrenoEnMemoria()`. En `src/app/terrenos/terrenos-client.tsx`, conexión del disparador `onNuevoLote`, actualización reactiva inmediata de la tabla (`terrenosList`, contadores de toolbar y selección con apertura automática en el sheet de inspección). Carga paralela de `initialPropietarios` en `src/app/terrenos/page.tsx`. 5) **Validación y Calidad:** 0 errores en TypeScript (`npx tsc --noEmit`), 0 advertencias en ESLint (`npm run lint`), build de producción aprobado (`npm run build` con 13/13 rutas estáticas generadas) y verificación HTTP 200 OK con el servidor de desarrollo activo.
2. **[2026-09-06 01:18] Vinculación de Remoto GitHub y Push Inicial Exitoso de Rama Main:** Configuración de origen remoto `https://github.com/salempc-pe/central-promundo.git` y sincronización upstream `git push -u origin main` completada con éxito. Código fuente, esquemas Drizzle, configuración Next.js 14 y suites de validación 100% disponibles en GitHub con exclusión estricta de variables de entorno y credenciales sensibles (`.env.local`).
3. **[2026-09-06 01:16] Conexión Integral de Supabase PostgreSQL + PostGIS, Ejecución de Migración DDL y Validación de Build de Producción:** Configuración de `.env.local` con credenciales de Supabase (`rioosacxbuwkxntxmmpe`). Ejecución exitosa de migración DDL (`scripts/apply-migration.mjs` y `0000_init_postgis_schema.sql`) habilitando extensiones `postgis` y `uuid-ossp`, 8 tablas (`terrenos`, `documentos_terreno`, `propietarios`, `clientes`, `negociaciones`, `bitacora_negociacion`, `comisiones_cierres`, `usuarios`), triggers para cálculo de punto espacial WGS84 e índices GiST y B-Tree. Inicialización de Git con rama `main`, blindaje de credenciales en `.gitignore`, verificación de `npm run build` (13/13 rutas estáticas compiladas exitosamente con 0 errores) y commit inicial para despliegue en Vercel.

### 📅 2026-09-05
4. **[2026-09-05 19:55] Erradicación de la Sensación de Vista 3D (Transición a 2D Planar con easeTo) y Desacoplamiento de Clic en Marcador vs Sheet Lateral:** Investigación forense de cinemática de cámara MapLibre (`systematic-debugging` y `uxui-principles`). 1) **Causa de la Sensación 3D:** El método `map.flyTo` implementa el algoritmo de van Wijk (2003) de vuelo parabólico espacial (zoom-out para ganar altitud y swoop dive en picada); combinado con el Field of View ($\approx 37^\circ-53^\circ$) de la proyección WebGL, genera un campo de flujo óptico con distorsión periférica que el ojo percibe idéntico a un descenso inclinado desde un costado ("efecto de vista 3D"). Reemplazo de `map.flyTo` por `map.easeTo` (`zoom: 16.0`, `duration: 500ms`, `easing: quad ease-out`), logrando un desplazamiento y zoom 100% planos sobre el plano 2D perpendicular (vista cenital pura sin swoop ni cambio de altitud). Incorporación de `linear: true` en todas las llamadas a `map.fitBounds` (cargas y auto-encuadre). 2) **Desacoplamiento de Interacción UX:** En `terrenos-map.tsx` y `mapa-client.tsx`, se desacopló el clic del marcador de la apertura del Sheet. Anteriormente, pulsar un pin forzaba de inmediato la apertura del Sheet drawer de 672px con un backdrop oscuro (`fixed inset-0 bg-slate-800/40 backdrop-blur`), oscureciendo el mapa y tapando el propio Popup HUD. Con la refactorización, el clic sobre el pin únicamente enfoca el predio en 2D y despliega el Popup HUD de 360px manteniendo el mapa iluminado; la apertura profunda del Sheet lateral se reserva exclusivamente para la pulsación de "Abrir Inspección Técnica" dentro del Popup o los botones "Ficha" / "Matching" del listado lateral `TerrenosSplitList`. Validación completa con 0 errores TypeScript (`npx tsc --noEmit`), 0 advertencias en ESLint (`npm run lint`), 18/18 PASS en integridad geoespacial y confirmación HTTP 200 OK en `/mapa`.
5. **[2026-09-05 18:55] Micro-Calibración Cartográfica Final, Corrección de Regex en Script de Integridad y Bloqueo Total Multi-Input de Pitch:** Auditoría forense independiente y verificación exhaustiva de coordenadas contra OpenStreetMap y Nominatim. 1) **Alineación de TR-SBOR-071 (Av. San Borja Sur 890):** Confirmada y blindada la ubicación exacta sobre el eje de Av. San Borja Sur cuadra 8 (`-12.1008800, -76.9928500`), a 2 cuadras del Pentagonito y Av. Boulevard (corrigiendo la ubicación errónea que aparecía al sur en la captura del usuario). 2) **Corrección de Terrenos Adicionales en terrenos-seed.ts:** Detección y corrección de discrepancias en `TR-MIRA-084` (reubicado a cuadra 6 de Balboa en el número 615/640: `-12.1306000, -77.0251000`), `TR-LMOL-039` (reubicado de cuadra 11 a cuadra 5 frente al Campus USIL: `-12.0728000, -76.9538000`) y `TR-ATE-090` (ajustado a la acera sur de Av. Nicolás Ayllón en el distrito de Ate: `-12.0592000, -76.9742000`). 3) **Bloqueo Total de Perspectiva 3D y Pitch en terrenos-map.tsx:** Incorporación de `minPitch: 0` en constructor de MapLibre, deshabilitación de controles por teclado `map.keyboard.disableRotation()`, adición de `pitch: 0` y `bearing: 0` en todos los `fitBounds`, y listener de evento defensivo `map.on("pitch", ...)` forzando 0° ante cualquier intento de inclinación. 4) **Corrección de Regex en Script de Integridad:** Corrección del regex en `scripts/verify-spatial-integrity.mjs` para capturar estrictamente IDs `tr-XX` (eliminando colisión con IDs de documentos `doc-XX`), incorporando además verificaciones de cuadrantes para Balboa, La Fontana (USIL) y Nicolás Ayllón (Ate). Verificación con 18/18 PASS (100%), `npm run lint` (0 errores) y `npx tsc --noEmit` (0 errores).
6. **[2026-09-05 18:40] Calibración Cartográfica 100% Vial de 18 Terrenos y Bloqueo Total de Perspectiva 3D/Pitch Cenital en MapLibre GL JS:** Investigación geográfica profunda con OpenStreetMap y geocodificación inversa/directa para alinear con exactitud milimétrica los 18 lotes de `src/lib/mock/terrenos-seed.ts`. 1) **Alineación de TR-SBOR-071 (Av. San Borja Sur 890):** Corregido el desplazamiento erróneo de 400 metros al sur (que caía en Calle Pablo Usandizaga / Jr. Maestro Chueca en la captura del usuario) reubicándolo exactamente sobre el eje de la Av. San Borja Sur cuadra 8 (`-12.1008800, -76.9928500`), a 2 cuadras del Pentagonito y Av. Boulevard. 2) **Auditoría Vial Integral de los 18 Terrenos:** Recalibración completa de todos los predios verificados con reverse geocoding en OpenStreetMap alcanzando 18/18 aciertos directos en su respectiva vía (incluyendo `TR-MIRA-084` en Av. Balboa, `TR-JMA-033` en Av. San Felipe frente a UP y Canal 2, `TR-LCE-014` en Av. Arequipa 2450 frente a Parque Castilla, `TR-MAG-058` en Jr. Salaverry 410, `TR-SMIG-092` en Costanera 1100, `TR-BCO-045` en Av. Pedro de Osma 180, `TR-LMOL-039` en Av. La Fontana 520, etc.). 3) **Bloqueo Inmutable de Pitch 3D:** En `src/components/mapa/terrenos-map.tsx`, eliminación de la inclinación oblicua (`pitch: 35` en `flyTo` al seleccionar predio) estableciendo `pitch: 0` cenital estricto, configuración en constructor MapLibre de `pitch: 0`, `maxPitch: 0`, `dragRotate: false`, `touchPitch: false`, `pitchWithRotate: false` y deshabilitación explícita de `dragRotate`, `touchPitch` y rotación en `touchZoomRotate`. 4) **Validación y Suite:** Enriquecimiento de `scripts/verify-spatial-integrity.mjs` con verificación de corredores viales exactos y costa de San Miguel (18/18 PASS 100%). Verificación con `npm run lint` (0 errores) y `npx tsc --noEmit` (0 errores).
7. **[2026-09-05 18:20] Alineación Vial de TR-SISI-019 (Calle Las Palmeras), Rediseño de Popup HUD a 360px y Creación de Validador Geoespacial:** Investigación profunda mediante subagentes especializados (Especialista Cartográfico y Diseñador UI/UX) utilizando `systematic-debugging` y `uxui-principles`. 1) **Alineación Vial:** Corrección del predio `tr-002` (Calle Las Palmeras 380, San Isidro, `TR-SISI-019`), reubicado desde la vía privada "Vía Real" del Centro Empresarial Real a su eje oficial en Calle Las Palmeras cdra 3 (`-12.0944500, -77.0386500`), junto con micro-calibraciones viales en `tr-001` (Balboa), `tr-008` (Arequipa), `tr-010` (Principal), `tr-012` (2 de Mayo), `tr-013` (Encalada) y `tr-014` (Rivera Navarrete). 2) **Rediseño del Popup HUD:** Detección de la restricción oculta de MapLibre GL JS (`max-width: 240px` por defecto). Expansión del popup a 360px (`maxWidth: "360px"` en constructor y en `globals.css`), creación de zona de exclusión (`pr-8`, 32px) para aislar el botón `[X]` sin superposición sobre el badge `CZ` ni el estado comercial, erradicación de `truncate` rígido en dirección y referencia permitiendo lectura multilínea completa (`line-clamp-2`, `break-words`). 3) **Blindaje Geoespacial Futuro:** Creación del módulo de validación catastral `src/lib/map/geo-validator.ts` con Bounding Boxes oficiales para 15 distritos de Lima y detección de puntos marítimos, y suite automatizada `scripts/verify-spatial-integrity.mjs` con 18/18 lotes aprobados al 100%. Validación con `npx tsc --noEmit` (0 errores), `npm run lint` (0 advertencias) y HTTP 200 en `/mapa`.
8. **[2026-09-05 17:42] Optimización de Rendimiento a 60fps en MapLibre GL JS y Recalibración Cartográfica WGS84 de Terrenos:** Investigación previa con subagentes especializados (`systematic-debugging`, `phase-gated-debugging`, GIS/Real Estate y Arquitectura Frontend). Identificación y corrección de dos problemas críticos en el Módulo B: 1) **Lag severo al arrastrar:** En `src/components/mapa/terrenos-map.tsx`, el nodo DOM raíz del marcador (`el`) incluía `transition-all duration-200 / duration-150`, interceptando las escrituras inline de `style.transform` enviadas por MapLibre a 60 fps y retrasando los pines de manera continua hasta que el usuario soltaba el mouse. Implementación del patrón de Aislamiento DOM de Marcadores: el nodo raíz `el` permanece limpio (`cursor-pointer select-none`), mientras que las transiciones de escala (`scale-105`, `scale-110`, `hover:scale-105`) se delegaron a un contenedor hijo interno `.pin-container`, alcanzando paneo instantáneo a 60 fps sin retardo. 2) **Terrenos en el océano y acantilados:** En `src/lib/mock/terrenos-seed.ts`, recalibración completa de coordenadas WGS84 (`latitud`, `longitud` y objeto `geom`) para situar todos los lotes en tierra firme: `tr-005` (San Miguel, Costanera 1100) retirado 1.4 km del mar a su manzana urbana (`-12.0848, -77.0879`), `tr-007` (Magdalena del Mar, Jr. Salaverry 410) retirado del acantilado a su ubicación real (`-12.0912, -77.0694`), `tr-015` (Pueblo Libre) alineado a la Av. Sucre (`-12.0772, -77.0645`), `tr-008` (Lince) corregido en su límite distrital (`-12.0833, -77.0340`), `tr-003` (Surco) subido 1.3 km al eje El Derby (`-12.1008, -76.9742`) y saneada la referencia de `tr-012` (Miraflores). Verificación de compilación TypeScript con 0 errores (`npx tsc --noEmit`), linter aprobado con 0 warnings (`npm run lint`) y respuesta HTTP 200 en `/mapa`.

### 📅 2026-09-04
9. **[2026-09-04 18:43] Desacoplamiento de Ciclo de Vida en MapLibre GL JS, Retención Estricta de Viewport y Estandarización Light Theme de Marcadores:** Investigación profunda previa mediante subagentes especializados (`systematic-debugging` y `uxui-principles`) para erradicar el reseteo involuntario de zoom (zoom out a Lima metropolitana). Identificación de causa raíz en `src/components/mapa/terrenos-map.tsx`: acoplamiento de `hoveredTerrenoId` en dependencias del `useEffect` de marcadores y ejecución incondicional de `map.fitBounds(bounds)` ante cualquier hover/unhover en pines o tarjetas de `TerrenosSplitList`. Refactorización arquitectónica en 3 efectos ortogonales independientes: 1) Creación y reconciliación de marcadores (`[terrenos, isMapLoaded]`) desacoplada de callbacks mediante refs (`onSelectTerrenoRef`, `onHoverTerrenoRef`, `selectedTerrenoIdRef`); 2) Actualización visual atómica de hover y selección (`[selectedTerrenoId, hoveredTerrenoId]`) manipulando estilos directos sobre el DOM en GPU (`scale`, `z-index`, bordes y sombras) con cero afectación de cámara y cero recreación de marcadores; 3) Control de cámara inteligente (`[terrenos, isMapLoaded]`) con banderas `hasInitialFittedRef` y `prevTerrenosKeyRef` que solo dispara `fitBounds` en la carga inicial o ante un cambio real en los filtros aplicados y sin lote seleccionado. Sincronización en `src/app/mapa/mapa-client.tsx` con `useCallback` para estabilizar referencias de `handleSelectTerreno` y `handleHoverTerreno`. Estandarización de pines a Bloomberg Light Theme estricto (fondo blanco `#ffffff`, borde pizarra `#cbd5e1`, tipografía oscura `$3,250/m²` y badges de zonificación pastel RDA/CZ/CM/RDM). Verificación exitosa con `npx tsc --noEmit` (0 errores), `npm run lint` (0 advertencias) y HTTP 200 OK en `/mapa`.
10. **[2026-09-04 14:46] Depuración Integral de Enlaces Inter-Módulos, Sincronización Pipeline/Comisiones/Auditoría y Verificación E2E 100%:** Corrección en `src/lib/services/negociaciones.ts` para permitir búsqueda libre por ID de deal (`neg-001`), ID de terreno (`tr-001`, `terr-001`), ID normalizado y propietario. Sincronización en `src/app/pipeline/pipeline-client.tsx` del query parameter `terrenoId` con el estado `busqueda` de la barra de herramientas. Sincronización en `src/app/matching/matching-client.tsx` para filtrar por `terreno.id` y `normQ` en vista cliente. Enlace con dealId en `src/components/comisiones/comision-detail-sheet.tsx` (`/pipeline?dealId=...`). Sobrescritura directa de `busqueda` en `/mapa` y `/terrenos` ante nuevos query parameters. Normalización de ID en `src/app/documentos/documentos-client.tsx`. Soporte de `useSearchParams` con `<Suspense>` en `/auditoria` (`q`, `entidadId`, `modulo`) y `/reportes` (`brokerId`, `periodo`). Cierre selectivo de dropdowns (`closeOnSelect={true}`) en filtros single-select de brokers. Suite E2E con Chrome CDP (`scripts/verify-comprehensive-e2e.mjs`) aprobada con 10/10 tests (100% PASS, exit code 0) y validación completa con `npm run lint` y `npx tsc --noEmit` (0 errores).

---

## 📌 SECCIÓN DE PENDIENTES Y HOJA DE RUTA

### 🟢 Módulos Base Completados (Fase 1):
- [x] **Módulo A: Data Grid de Terrenos con TanStack Table v8** 🟢 *(Completado y Validado)*
- [x] **Módulo B: Mapa Interactivo Georreferenciado** 🟢 *(Completado y Validado)*
- [x] **Módulo C: Gestión Documental & Certificados de Parámetros** 🟢 *(Completado y Validado)*
- [x] **Módulo D: Pipeline de Negociaciones & Bitácora de Auditoría** 🟢 *(Completado y Validado)*
- [x] **Módulo E: Motor de Matching Automático Lote ↔ Constructoras** 🟢 *(Completado y Validado)*
- [x] **Módulo F: Comisiones, Liquidaciones y Reportes Financieros** 🟢 *(Completado y Validado)*
- [x] **Módulo G: Métricas, Rendimiento BI & Reportes Ejecutivos (`/reportes`)** 🟢 *(Completado y Validado)*
- [x] **Módulo H: Configuración GIS, Parámetros Normativos & Auditoría Global (`/configuracion`, `/auditoria`)** 🟢 *(Completado y Validado)*

### 🟡 Iniciativas y Nuevos Módulos en Hoja de Ruta (Fase 2):
- [ ] **Fase 2.1: Unificación Reactiva del Dashboard Principal (`/`) & Command Palette Global (`Ctrl+K`)** 🟡 *(Prioridad Alta - Próximo)*
  - [ ] Conectar Dashboard (`src/app/page.tsx`) a los servicios reactivos del sistema (`terrenosService`, `negociacionesService`, `documentosService`, `matchingService`, `comisionesService`, `auditoriaService`).
  - [ ] KPIs dinámicos en tiempo real (valor real de cartera, área total en gestión, lotes activos, alertas reales de vencimiento de certificados).
  - [ ] Sincronizar ticker del `Header` con el inventario y estado global en tiempo real.
  - [ ] Implementar diálogo interactivo Command Palette (`Ctrl+K`) tipo Spotlight para búsqueda rápida de lotes, constructoras, documentos y navegación directa con atajos de teclado.

- [ ] **Fase 2.2: Módulo I: Directorio de Constructoras, Desarrolladores y Mandatos (`/constructoras` o `/clientes`)**
  - [ ] Tipado estricto e interfaces para constructoras, perfiles de inversión y mandatos de búsqueda (`src/types/constructoras.ts`).
  - [ ] Data Grid TanStack Table v8 con filtros por ticket, distritos prioritarios y capacidad de absorción.
  - [ ] Ficha técnica del desarrollador con mandatos activos e historial de lotes presentados/evaluados.
  - [ ] Integración comercial directa con el motor de matching y el pipeline de negociaciones.

- [x] **Fase 2.3: Formulario Integral de Alta de Nuevos Lotes (`TerrenoCreateDialog`)** 🟢 *(Completado y Validado)*
  - [x] Modal de alta corporativo (Bloomberg Light) con código sugerido por distrito (`TR-[DIST]-[SEQ]`), titular SUNARP y parámetros técnicos.
  - [x] Georreferenciación WGS84 con centroides referenciales distritales y validación territorial estricta (`validateTerrenoCoordinates`).
  - [x] Cálculo financiero bidireccional reactivo entre precio total (USD), área (m²) y precio/m² ($/m²).
  - [x] Registro rápido inline de nuevos propietarios con tipo y número de documento (DNI/RUC) y apoderado.
  - [x] Conexión reactiva en `TerrenosClient` con TanStack Table v8 y selección automática tras el guardado.

- [ ] **Fase 2.4: Generador de Ficha Comercial & Dossier Ejecutivo de Inversión en PDF**
  - [ ] Diseño y maquetación de One-Pager / Teaser Comercial imprimible y descargable en formato PDF de alta fidelidad.
  - [ ] Inclusión de ficha técnica de lote, croquis/ubicación satelital, resumen de parámetros distritales y análisis económico preliminar (incidencia estimada de suelo).
  - [ ] Modal de previsualización e impresión directa para comités de inversión.

- [ ] **Fase 2.5: Persistencia en Base de Datos Real (Supabase / PostgreSQL + PostGIS)**
  - [x] Esquema DDL, extensiones PostGIS (`EPSG:4326`), triggers e índices creados y verificados en Supabase Cloud (`rioosacxbuwkxntxmmpe`). 🟢 *(Completado)*
  - [ ] Conexión a Supabase/PostgreSQL mediante Server Actions o endpoints API integrados con Drizzle ORM.
  - [ ] Consultas espaciales nativas en PostGIS (`ST_DWithin`, `ST_Intersects`, buffers territoriales distritales).
  - [ ] Sincronización y persistencia bidireccional entre la base de datos y los estados locales de la interfaz.


---

## 🟢 ESTADO DEL ENTORNO
* **Servidor Dev:** `http://localhost:3000` (Activo y corriendo en background).
* **Compilación TS:** 0 errores (`npx tsc --noEmit` verificado).
* **Base de Datos:** Drizzle ORM configurado con soporte PostGIS (`EPSG:4326`).
