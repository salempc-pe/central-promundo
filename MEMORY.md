# MEMORY.md - Memoria Operativa & Handoff de "Promundo Sistema"

Este archivo mantiene el estado vivo del proyecto, el historial rotativo de las últimas 10 acciones ejecutadas (con fecha y hora) y el mapa de pendientes según lo estipulado en `AGENTS.md`.

---

### 🕒 ÚLTIMAS 10 ACCIONES EJECUTADAS (ROTATIVO)

###### 📅 2026-09-07
1. **[2026-09-07 09:54] Restauración y Diagnóstico Forense de la Pipeline CSS y Dev Server tras Build de Producción:** Diagnóstico y solución mediante subagentes especializados (`systematic-debugging`, `lint-and-validate`, `clean-code`). 1) **Causa Raíz de Pérdida de Estilos:** Tras ejecutar `npm run build` mientras el proceso `next dev` seguía activo, Next.js purgó y regeneró `.next` con la estructura hash de producción, suprimiendo los archivos CSS temporales de desarrollo (`.next/static/css/app/layout.css`). El proceso en memoria de `next dev` continuó solicitando la ruta de desarrollo inexistente arrojando HTTP 404, provocando que el navegador renderizara HTML plano sin estilos de Tailwind CSS. 2) **Resolución Limpia:** Terminación forzada de procesos Node residuales, purga completa del directorio de caché `.next` e inicio en limpio de `npm run dev` en segundo plano permanente como daemon. 3) **Verificación:** Inspección HTTP con PowerShell validando que `http://localhost:3000` entrega el HTML correcto y `http://localhost:3000/_next/static/css/app/layout.css` responde HTTP 200 OK con el bundle completo de Tailwind CSS (94 KB).
2. **[2026-09-07 09:18] Incorporación del Catálogo Maestro Institucional de Zonificaciones Oficiales de Lima Metropolitana (MML / RATUS) y Expansión Integral en Selectores, Filtros y Matching:** Investigación previa con subagente especializado (`clean-code`, `typescript-pro`, `design-taste-frontend`, `lint-and-validate`). 1) **Normativa y Taxonomía Urbana de Lima:** Investigación basada en Ordenanza 1076-MML, RATUS (DS 012-2021-VIVIENDA) y parámetros distritales oficiales. Incorporación de 21 códigos canónicos agrupados en: Residenciales (`RDB`, `RDM`, `RDA`, `RDMA`, `VT`), Comerciales (`CV`, `CZ`, `CM`, `CI`, `CE`), Industriales (`I1`, `I2`, `I3`, `I4`), y Equipamientos / Especiales (`OU`, `ZRE`, `ZTE`, `E`, `H`, `ZRP`, `CH`). 2) **Catálogo Maestro Centralizado (`src/lib/constants/zonificaciones.ts`):** Creación del archivo canónico exportando `ZONIFICACIONES_LIMA`, `ZONIFICACIONES_CODES`, `ZONIFICACIONES_PRIORITARIAS_FILTRO`, `ZONIFICACIONES_POR_CATEGORIA`, `getZonificacionBadgeClass()` y `getZonificacionLabel()`. 3) **Selector en Alta de Lote (`TerrenoCreateDialog`):** Refactorización del dropdown de zonificación utilizando `SelectGroup` y `SelectLabel` por categorías ordenadas, permitiendo registrar predios con cualquiera de las zonificaciones oficiales (`RDMA`, `I2`, `RDM`, etc.) con nombres descriptivos. 4) **Filtros Dinámicos en Inventario (`TerrenosFilters`):** Ampliación del panel lateral de filtros con chips prioritarios y toggle expandible para acceder a las 21 zonificaciones con contador reactivo de selección. 5) **Ficha Técnica y Modos de Edición (`TerrenoDetailSheet`):** Conversión del campo de edición de zonificación a selector agrupado oficial y enriquecimiento del modo lectura con badges temáticos y tooltips normativos. 6) **Data Grid y Split View (`TerrenosColumns` & `TerrenosSplitList`):** Estandarización de badges corporativos con paleta pastel Bloomberg Light. 7) **Motor de Matching (`matching.ts`):** Calibración de absorción residencial e industrial reconociendo `RDMA` e `I2`. 8) **Validación:** 0 errores TypeScript (`npx tsc --noEmit`), 0 advertencias en ESLint (`npm run lint`), respuestas HTTP 200 OK y servidor de desarrollo activo.
3. **[2026-09-07 08:55] Migración Fiduciaria Integral a PostgreSQL, Erradicación Total de Mocks y Sincronización en Producción:** Auditoría, refactor y verificación integral mediante subagentes especializados (`clean-code`, `database-design`, `postgresql`, `nextjs-app-router-patterns`, `react-patterns`, `lint-and-validate`). 1) **Sembrado Completo y Consistente en Supabase Cloud:** Ejecución exitosa de `scripts/seed-complete-db.mjs` poblando de forma relacional 8 tablas en PostgreSQL con PostGIS SRID 4326 (`usuarios`: 6, `propietarios`: 6, `terrenos`: 20 lotes referenciados, `clientes`: 4 constructoras, `documentos_terreno`: 15 documentos técnicos con CPUs, `negociaciones`: 10 deals en pipeline, `bitacora_negociacion`: 31 eventos inmutables de auditoría, `comisiones_cierres`: 2 liquidaciones fiduciarias). 2) **Auditoría Automatizada de Integridad:** Ejecución de `scripts/verify-full-db-integrity.mjs` con 11/11 verificaciones superadas (100% PASS, 0 fallos), validando conteos, integridad de claves foráneas y coordenadas geográficas. 3) **Erradicación Total de Mocks y Stores en Memoria:** Eliminación del 100% de imports desde `@/lib/mock` en toda la carpeta `src/`. Parámetros urbanísticos normativos migrados a constantes reales en `src/lib/constants/parametros-normativos.ts`. Purga de arrays volátiles en memoria Node.js en todos los servicios (`terrenos`, `negociaciones`, `documentos`, `comisiones`, `auth-service`, `configuracion`, `matching`). 4) **Arquitectura de Server Actions con Drizzle ORM:** Creación y unificación de acciones en `src/lib/actions/` (`terrenos-actions.ts`, `pipeline-actions.ts`, `documentos-actions.ts`, `comisiones-actions.ts`, `reportes-actions.ts`, `matching-actions.ts`, `auditoria-actions.ts`, `dashboard-actions.ts`). Conexión de todas las vistas (`/`, `/terrenos`, `/mapa`, `/pipeline`, `/documentos`, `/matching`, `/comisiones`, `/reportes`, `/auditoria`) a datos reales fiduciarios con invalidación de caché (`revalidatePath`). 5) **Validación y Build de Producción:** Resolución de tipado estricto (`npx tsc --noEmit` código 0), linter sin errores y compilación de producción exitosa (`npm run build` con 17/17 páginas estáticas y dinámicas compiladas). Servidor de desarrollo levantado y permanente según `AGENTS.md`.
4. **[2026-09-07 00:25] Implementación de Eliminación Fiduciaria Segura de Terrenos con Modal de Confirmación y Reactividad Multi-Módulo:** Diagnóstico y solución mediante subagentes especializados (`clean-code`, `react-patterns`, `nextjs-best-practices`, `postgresql`, `lint-and-validate`). 1) **Causa Raíz:** No existía mecanismo en la aplicación para eliminar terrenos del catálogo institucional. El usuario solicitó explícitamente agregar la opción en el modal de ficha técnica bajo los botones de mapa. 2) **Server Action con Blindaje Fiduciario:** Creación de `deleteTerrenoAction` en `src/lib/actions/terrenos-actions.ts`. Incluye pre-flight check sobre la tabla `negociaciones`: si el terreno tiene negociaciones vinculadas en el Pipeline Comercial (`ON DELETE RESTRICT`), la acción bloquea la eliminación con una advertencia en español protegiendo la auditoría de bitácoras y comisiones. Si no tiene negociaciones, ejecuta una transacción atómica eliminando documentos asociados (`documentos_terreno`) y el lote de terreno, preservando al titular en `propietarios`. Invalida caché de Next.js (`/terrenos`, `/mapa`, `/`, `/pipeline`, `/documentos`). En `terrenos.ts`, adición de `deleteTerrenoEnMemoria` para consistencia. 3) **Botón Destructivo y Diálogo Fiduciario Bloomberg Light en Ficha Técnica (`TerrenoDetailSheet`):** Incorporación del botón "Eliminar Terreno" con estilo Bloomberg Light (`bg-rose-50 text-rose-700 hover:bg-rose-100 border border-rose-200`) ubicado en la pestaña *Ficha Técnica* inmediatamente bajo los botones de mapas (`Ver en Mapa Geoespacial del Sistema` y `Abrir en Google Maps Externo`). Despliegue de un diálogo modal (`Dialog` de Radix UI / Bloomberg Light) con resumen del activo (código, dirección, área, precio, documentos afectados), advertencia clara si tiene negociaciones bloqueantes, checkbox obligatorio de confirmación fiduciaria y botón de confirmación con spinner `Loader2`. 4) **Sincronización Reactiva Multi-Módulo:** Incorporación del prop `onTerrenoDeleted?: (terrenoId: string) => void` en `TerrenoDetailSheet`. En `TerrenosClient`, filtrado inmediato de `terrenosList` y limpieza de selección. En `MapaClient`, habilitación de `setTerrenosList`, sincronización con `initialTerrenos` y filtrado reactivo. En `DocumentosClient`, limpieza de selección y recarga de documentos. 5) **Verificación Automatizada y E2E:** Ejecución de `node scripts/test-delete-terreno.mjs` validando inserción, cascade en documentos, bloqueo preventivo con negociaciones activas y purga limpia en PostgreSQL (100% PASS). Compilación estricta con `npx tsc --noEmit` (0 errores), linter con `npm run lint` (0 errores, 0 advertencias) y verificación HTTP 200 OK en `/terrenos`, `/mapa` y `/documentos`.
5. **[2026-09-07 00:05] Solución Integral de Visibilidad Inmediata y Reactividad en Alta de Terrenos (TerrenoCreateDialog & TerrenosClient):** Diagnóstico y solución mediante subagentes especializados (`systematic-debugging`, `react-patterns`, `nextjs-best-practices`, `lint-and-validate`). 1) **Causa Raíz de Desaparición Aparente:** En `TerrenosClient`, el estado de TanStack Table tenía configurado un ordenamiento fijo alfabético por código (`sorting: [{ id: "codigoInterno", desc: false }]`) y paginación fija en 15 filas (`pageSize: 15`). Al registrar un nuevo terreno (ej. `TR-SURC-020` o `TR-SURC-021`), aunque `createTerrenoAction` lo insertaba correctamente en Supabase PostgreSQL con PostGIS y `handleCreateTerreno` lo agregaba en el índice 0 del estado local (`setTerrenosList([nuevo, ...prev])`), TanStack Table aplicaba `getSortedRowModel()`, reordenando todo alfabéticamente. Dado que el código alfabético de Surco o San Isidro quedaba en la posición 17-18, el registro era desplazado a la **Página 2** del Data Grid. Como la tabla permanecía en la Página 1, el usuario no veía el terreno y asumía que no se había guardado. Asimismo, el `useEffect` de sincronización con `searchParams` dependía de `terrenosList`, re-aplicando filtros de URL obsoletos tras la inserción, y si el usuario tenía filtros distritales o de texto activos, el nuevo terreno era excluido de `filteredTerrenos`. Además, en `TerrenoCreateDialog`, la generación de código sugerido podía colisionar con códigos existentes de seed (ej. `TR-SISI-019`), y los errores de validación no realizaban scroll hacia la alerta en el formulario. 2) **Ordenamiento Natural y Paginación Fila 1:** En `TerrenosClient`, se cambió el ordenamiento inicial a `sorting: []` (respetando el orden natural de la base de datos `orderBy: [desc(terrenos.createdAt)]`, más recientes primero). En `handleCreateTerreno`: se limpian los filtros acumulativos (`setFiltros({})`), se restablece el ordenamiento (`setSorting([])`), se fuerza la tabla a la primera página (`table.setPageIndex(0)`), se añade al inicio del listado y se resalta la fila (`setSelectedTerreno`). 3) **Banner de Confirmación Bloomberg Light:** Incorporación de micro-banner corporativo (`bg-emerald-50/90 border-emerald-200 text-emerald-800`) sobre el Data Grid notificando que el lote fue creado exitosamente y posicionado en la primera fila, con botón rápido "Abrir Ficha Técnica". 4) **Unicidad de Código Sugerido y Auto-Scroll en Diálogo:** En `TerrenoCreateDialog`, se incorporó `codigosExistentes` para que el generador correlativo detecte códigos ocupados e incremente el correlativo automáticamente hasta encontrar uno libre (garantizando 0 colisiones en PostgreSQL). Se añadió `formRef` con auto-scroll suave hacia el mensaje de error en caso de fallo, y reseteo completo del formulario (`resetForm()`) tras guardar con éxito. En `createTerrenoAction`, cálculo defensivo de `precioM2` y captura amigable de violación de clave única `23505`. 5) **Sincronización Server-Client & Desacoplamiento de URL:** Se implementó `terrenosListRef` para desacoplar el efecto de `searchParams` de las mutaciones de inventario, y `useEffect` para sincronizar `initialTerrenos` e `initialPropietarios` ante revalidaciones de Next.js. 6) **Verificación:** `npx tsc --noEmit` completado con 0 errores, `npm run lint` con 0 advertencias y 0 errores, servidor dev activo en puerto 3000 con HTTP 200 OK.

###### 📅 2026-09-06
6. **[2026-09-06 21:15] Blindaje Definitivo de Selección de Broker en DealCreateDialog y Restauración de Servidor Dev Permanente:** Diagnóstico y solución mediante subagentes especializados (`clean-code`, `react-patterns`, `nextjs-best-practices`, `lint-and-validate`). 1) **Causa Raíz de Reversión de Broker:** En `DealCreateDialog`, el `useEffect` de broker dependía de `[isOpen, currentUserId, brokers]` sin bandera de apertura. Al seleccionar otro broker, la resolución asíncrona de `currentUser` o re-renders de `PipelineClient` disparaban nuevamente el efecto, ejecutando `setBrokerId(currentUserId)` y forzando la selección de regreso a Paulo Salem. 2) **Blindaje con Flag de Ciclo de Vida (`hasInitializedRef`):** Se implementó una referencia mutable (`hasInitializedRef`) que solo inicializa al usuario por defecto una única vez cuando el modal pasa de cerrado a abierto. Una vez abierto, cualquier selección manual de broker en el dropdown se preserva de forma inmutable sin ser sobreescrita por re-renders. 3) **Estabilización de DealDetailSheet y PipelineClient:** En `DealDetailSheet`, se agregó `prevDealIdRef` para evitar reseteos de broker en bitácora durante la inspección del mismo deal. En `PipelineClient`, se removió la llamada redundante a `setBrokers` tras el montaje. 4) **Restauración y Verificación del Servidor Next.js:** El servidor de desarrollo fue levantado en segundo plano en `http://localhost:3000`. Se verificó que todos los chunks JS/CSS de `_next/static` responden HTTP 200 OK tanto en `/` como en `/login` y `/pipeline`. 5) **Validación:** 0 errores en TypeScript (`npx tsc --noEmit`), 0 errores y advertencias en ESLint (`npm run lint`).
7. **[2026-09-06 20:58] Estabilización de Ciclos de Renderizado en PipelineClient y Blindaje contra Congelamiento de UI:** Diagnóstico y solución mediante subagentes especializados (`clean-code`, `react-patterns`, `nextjs-best-practices`). 1) **Causa Raíz:** En `PipelineClient`, la función `loadData` incluía `selectedDeal` como dependencia en `useCallback` y al mismo tiempo llamaba a `setSelectedDeal(refreshed)`. Además, un `useEffect` sincronizaba `searchParams` y `deals`, actualizando `filtros` y `selectedDeal` cada vez que `deals` mutaba. Al recargar la página `/pipeline`, esta cascada de dependencias circulares provocaba ejecuciones continuas de re-renderizado, bloqueando el hilo principal de React en el navegador y provocando pantalla en blanco tras la recarga. 2) **Estabilización con Mutable Ref:** Se desacopló `selectedDeal` del `useCallback` de `loadData` utilizando `selectedDealRef`, garantizando que `loadData` solo se regenere cuando cambien los `filtros`. 3) **Separación de Efectos de URL:** Se dividió el efecto de `searchParams` en dos efectos específicos: uno para sincronizar filtros de URL y otro para resolver la preselección inicial del deal solicitado. 4) **Verificación:** `npx tsc --noEmit` completado con 0 errores, `npm run lint` con 0 errores y 0 advertencias, y servidor de desarrollo respondiendo HTTP 200 OK.
8. **[2026-09-06 20:51] Corrección de Retención de Selección de Broker en DealCreateDialog y Desacoplamiento de Ciclo de Vida Reactivo:** Diagnóstico y solución mediante subagentes especializados (`clean-code`, `react-patterns`, `nextjs-best-practices`). 1) **Causa Raíz:** En el modal de alta de negociaciones (`DealCreateDialog`), al seleccionar un broker diferente (ej. cualquier broker de la lista desplegable), el selector volvía de inmediato a "Paulo Salem". Esto ocurría porque un `React.useEffect` incluía `[brokers, currentUserId, isOpen, brokerId]` como dependencias y ejecutaba `if (currentUserId && brokers.some((b) => b.id === currentUserId)) setBrokerId(currentUserId);`. Cada vez que el usuario seleccionaba otro broker, cambiaba `brokerId`, disparando nuevamente el efecto y sobreescribiendo la selección con `currentUserId`. 2) **Desacoplamiento Reactivo:** Se refactorizó el `useEffect` para depender estrictamente de `[isOpen, currentUserId, brokers]`, inicializando al usuario actual únicamente al abrir el diálogo o si se cargan los brokers por primera vez. Una vez abierto el modal, la selección manual del usuario (`setBrokerId`) se preserva de forma inmutable hasta el envío del formulario o cierre del modal. 3) **Auditoría Preventiva en Modales Conexos:** Se auditó y optimizó `DealDetailSheet` para evitar comportamientos análogos en la bitácora con `selectedBrokerId`. 4) **Verificación:** `npx tsc --noEmit` completado con 0 errores, `npm run lint` con 0 errores y 0 advertencias, y servidor de desarrollo activo.
9. **[2026-09-06 19:55] Sincronización Integral de Usuarios Reales de Base de Datos (Brokers) en Pipeline Comercial y Erradicación de Mocks:** Diagnóstico y solución mediante subagentes especializados (`clean-code`, `react-patterns`, `nextjs-app-router-patterns`). 1) **Causa Raíz:** En el modal de alta de negociaciones (`DealCreateDialog`), el selector de "Broker Responsable" listaba nombres ficticios estáticos (`Alvaro Barrenechea`, `Sofia Mendoza`, `Diego Palacios`) provenientes del mock `mockUsuarios` en `negociaciones-seed.ts`. Asimismo, en `createNegociacion`, `updateEtapaNegociacion` y `addBitacoraEvento` existía un fallback que forzaba el ID a `"usr-01"`. 2) **Server Action de Obtención de Usuarios Reales:** Creación de `src/lib/actions/pipeline-actions.ts` con `getBrokersAction()`, consultando directamente en PostgreSQL/Supabase a los usuarios con `estadoAcceso = 'aprobado'` y `activo = true` (roles `admin`, `broker_senior`, `broker_junior`), ordenados alfabéticamente y con fallback de respaldo vía `authService.getUsuariosSolicitudes()`. 3) **Hidratación Servidor -> Cliente:** En `src/app/pipeline/page.tsx` (Server Component), precarga de `brokers` vía `getBrokersAction()` e inyección como `initialBrokers` en `PipelineClient`. En `PipelineClient`, estado reactivo sincronizado con `setBrokersStore()` y detección de sesión activa (`getCurrentUserSession()`). 4) **Experiencia de Usuario en `DealCreateDialog`:** Selector enriquecido con microbadges corporativos de rol (`admin`, `broker_senior`, `broker_junior`) bajo estándar estricto Bloomberg Light Theme, preselección automática del usuario logueado o primer broker real disponible, y eliminación absoluta de dependencias a `"usr-01"`. 5) **Blindaje Defensivo contra Nulos:** Incorporación de optional chaining y valores seguros en `deal-card.tsx` (`brokerNombre`, iniciales), `deal-detail-sheet.tsx` y `pipeline-table-view.tsx` para evitar caídas en pantalla si se visualizan deals históricos. 6) **Verificación:** 0 errores TypeScript (`npx tsc --noEmit`), 0 advertencias ESLint (`npm run lint`), build de producción aprobado (17/17 páginas generadas exitosamente) y respuesta HTTP 200 OK en `/pipeline`.
10. **[2026-09-06 19:15] Calibración Catastral Oficial de Bounding Boxes de Lima y Corrección de Falso Positivo Geoespacial en Miraflores Este:** Diagnóstico y solución mediante subagentes especializados (`clean-code`, `react-patterns`, `nextjs-best-practices`). 1) **Causa Raíz:** Al registrar un lote de suelo en "Gustavo Escudero 218" (Calle Teniente Coronel G. Escudero O., Miraflores, coords `[-12.1260165, -77.005606]`), el sistema bloqueaba el guardado arrojando `Alerta de Georreferenciación: La coordenada [-12.1260165, -77.005606] se encuentra fuera de los límites catastrales de Miraflores. Rango permitido Lat [-12.138, -12.108], Lng [-77.042, -77.018]`. Ocurría porque la definición previa en `geo-validator.ts` establecía un límite oriental arbitrario (`maxLng: -77.0180`), excluyendo erróneamente todo el sector este de Miraflores (zona Alexander Von Humboldt, La Aurora este, Benavides cdras 20-30 y Tomás Marsano hasta `-77.001`). 2) **Calibración Oficial de Bounding Boxes:** En `src/lib/map/geo-validator.ts` y `scripts/verify-spatial-integrity.mjs`, se actualizaron los 15 bounding boxes distritales de Lima Metropolitana contrastándolos con las coordenadas administrativas oficiales de OpenStreetMap/IGN e incorporando un margen de tolerancia de 500m (`tolerance = 0.005`) para cubrir avenidas limítrofes. Miraflores se calibró a `Lat [-12.1450, -12.1000], Lng [-77.0600, -76.9950]`, permitiendo el registro fluido de Gustavo Escudero y vías circundantes. 3) **Protección Costera y UX Asistida:** En `isCoordinateInPacificOcean`, se blindó la cota de San Miguel a `-77.094` para evitar falsos positivos en el Malecón Costanera. En `validateTerrenoCoordinates`, se implementó detección predictiva de distritos colindantes para asistir al operador en caso de desviaciones reales. 4) **Verificación:** 18/18 PASS en `scripts/verify-spatial-integrity.mjs`, validación unitaria de las coordenadas del usuario `{ valid: true }`, `npx tsc --noEmit` completado con 0 errores y `npm run lint` con 0 advertencias.
10. **[2026-09-06 16:08] Aislamiento Estricto de Límites Cliente-Servidor (Resolución de Error 'net' en Next.js App Router):** Diagnóstico y solución mediante subagentes especializados (`nextjs-app-router-patterns`, `nextjs-best-practices`, `clean-code`). 1) **Causa Raíz:** Al navegar a `/terrenos`, Next.js arrojaba `Build Error: Module not found: Can't resolve 'net'` en `./node_modules/postgres/src/connection.js`. Ocurría porque componentes cliente (`"use client"`: `src/app/terrenos/terrenos-client.tsx`, `src/components/terrenos/terreno-detail-sheet.tsx` y `src/app/documentos/documentos-client.tsx`) importaban funciones de `src/lib/services/terrenos.ts`, el cual importa `@/db` y el driver nativo de Node.js `postgres`. Webpack intentaba empaquetar el módulo de red de Node (`net`) en el bundle del navegador. 2) **Desacoplamiento Arquitectónico:** Creación de Server Actions con directiva `"use server"` en `src/lib/actions/terrenos-actions.ts`: `createPropietarioAction`, `getTerrenosAction` y `getTerrenoByIdAction`. En `terrenos-client.tsx`: erradicación de imports de `services/terrenos` reemplazándolos por `createPropietarioAction`. En `terreno-detail-sheet.tsx`: importación directa de `calcularMatchingTerreno` desde `src/lib/services/matching.ts` (módulo de cálculo puro sin acoplamiento a base de datos). En `documentos-client.tsx`: reemplazo de `getTerrenos` por `getTerrenosAction`. Con esto, `src/lib/services/terrenos.ts` queda reservado estrictamente para Server Components (`/terrenos/page.tsx` y `/mapa/page.tsx`). 3) **Verificación:** `npx tsc --noEmit` completado con 0 errores, `npm run lint` completado con 0 advertencias y 0 errores. Pruebas HTTP contra el servidor dev en `/terrenos`, `/mapa`, `/documentos`, `/pipeline` y `/matching` respondiendo HTTP 200 OK y 0 errores de compilación Webpack.

---

## 📌 SECCIÓN DE PENDIENTES Y HOJA DE RUTA

### 🟢 Módulos Base Completados (Fase 1):
- [x] **Módulo A: Data Grid de Terrenos con TanStack Table v8** 🟢 *(Completado y Validado)*
- [x] **Módulo B: Mapa Interactivo Georreferenciado** 🟢 *(Completado y Validado)*
- [x] **Módulo C: Gestión Documental & Certificados de Parámetros** 🟢 *(Completado y Validado)*
- [x] **Módulo D: Pipeline de Negociaciones & Bitácora de Auditoría** 🟢 *(Completado y Validado)*
- [x] **Módulo E: Motor de Matching Automático Lote ↔ Constructoras** 🟢 *(Completado y Validado)*
- [x] **Módulo F: Comisiones, Liquidaciones y Reportes Financieros** 🟢 *(Completado y Validado)*
- [x] **Módulo G: Métricas, Rendimiento BI & Reportes Ejecutivos (`/reportes`)** 🟢 *(Completado y Validado)*
- [x] **Módulo H: Configuración GIS, Parámetros Normativos & Auditoría Global (`/configuracion`, `/auditoria`)** 🟢 *(Completado y Validado)*

### 🟡 Iniciativas y Nuevos Módulos en Hoja de Ruta (Fase 2):
- [x] **Fase 2.0: Control de Accesos, Google OAuth & Flujo de Aprobación Fiduciaria** 🟢 *(Completado y Validado)*
  - [x] Página de Login institucional Bloomberg Light con Google Sign-In (`@supabase/ssr`).
  - [x] Flujo de cuarentena: nuevos usuarios redirigidos obligatoriamente a `/espera` sin acceso a cartera ni datos sensibles.
  - [x] Módulo administrativo `/accesos` para revisión, aprobación, denegación y asignación de roles (`broker_junior`, `broker_senior`, `admin`).
  - [x] Superadministrador inmutable: `paulosalem8@gmail.com` aprobado por defecto con rol `admin` y candado de protección.
  - [x] Desacoplamiento de layout con `AppShell`, protección perimetral con `middleware.ts`, badges en Sidebar y Header.

- [x] **Fase 2.3: Formulario Integral de Alta de Nuevos Lotes (`TerrenoCreateDialog`) con Mini-Mapa y Geocodificación** 🟢 *(Completado y Validado)*
  - [x] Modal de alta corporativo (Bloomberg Light) con código sugerido por distrito (`TR-[DIST]-[SEQ]`), titular SUNARP y parámetros técnicos.
  - [x] Mini-mapa interactivo de 220px (`TerrenoLocationPicker`) con estilo claro MapLibre GL JS Carto Positron.
  - [x] Buscador predictivo de direcciones sobre OpenStreetMap Nominatim acotado a Lima Metropolitana con autocompletado.
  - [x] Pin arrastrable interactivo (`cursor-grab`) y posicionamiento al hacer clic sobre el mapa para ajustar coordenadas sin digitar números.
  - [x] Georreferenciación WGS84 con validación territorial estricta (`validateTerrenoCoordinates`) en tiempo real (alerta de tierra firme vs. mar).
  - [x] Cálculo financiero bidireccional reactivo entre precio total (USD), área (m²) y precio/m² ($/m²).
  - [x] Registro rápido inline de nuevos propietarios con tipo y número de documento (DNI/RUC) y apoderado.
  - [x] Conexión reactiva en `TerrenosClient` con TanStack Table v8 y selección automática tras el guardado.

- [x] **Fase 2.5: Persistencia y Operación Integral en Base de Datos Real (Supabase / PostgreSQL + PostGIS)** 🟢 *(Completado y Validado)*
  - [x] Esquema DDL, extensiones PostGIS (`EPSG:4326`), triggers e índices creados y verificados en Supabase Cloud (`rioosacxbuwkxntxmmpe`).
  - [x] Sembrado de 8 tablas relacionales con 20 terrenos (PostGIS SRID 4326), 6 propietarios, 6 usuarios, 4 constructoras, 15 documentos, 10 negociaciones, 31 bitácoras y 2 liquidaciones de comisiones.
  - [x] Erradicación total de imports de `@/lib/mock` y stores en memoria volátil en toda la aplicación (`src/`).
  - [x] Migración de parámetros urbanísticos normativos oficiales a `src/lib/constants/parametros-normativos.ts`.
  - [x] Server Actions con Drizzle ORM en `src/lib/actions/` para todos los módulos (`terrenos`, `pipeline`, `documentos`, `comisiones`, `reportes`, `matching`, `auditoria`, `dashboard`).
  - [x] Conexión de todas las vistas (`/`, `/terrenos`, `/mapa`, `/pipeline`, `/documentos`, `/matching`, `/comisiones`, `/reportes`, `/auditoria`) a datos reales fiduciarios con invalidación de caché (`revalidatePath`).
  - [x] Suite de auditoría automatizada (`scripts/verify-full-db-integrity.mjs`) aprobada con 11/11 verificaciones (100% PASS).
  - [x] Verificación de compilación de producción exitosa (`npm run build`, 17/17 rutas estáticas y dinámicas).

- [ ] **Fase 2.1: Command Palette Global (`Ctrl+K`) & Atajos de Navegación** 🟡 *(Prioridad Alta - Próximo)*
  - [ ] Implementar diálogo interactivo Command Palette (`Ctrl+K`) tipo Spotlight para búsqueda rápida de lotes, constructoras, documentos y navegación directa con atajos de teclado.
  - [ ] Sincronizar ticker del `Header` con el inventario y estado global en tiempo real.

- [ ] **Fase 2.2: Módulo I: Directorio de Constructoras, Desarrolladores y Mandatos (`/constructoras` o `/clientes`)**
  - [ ] Tipado estricto e interfaces para constructoras, perfiles de inversión y mandatos de búsqueda (`src/types/constructoras.ts`).
  - [ ] Data Grid TanStack Table v8 con filtros por ticket, distritos prioritarios y capacidad de absorción.
  - [ ] Ficha técnica del desarrollador con mandatos activos e historial de lotes presentados/evaluados.
  - [ ] Integración comercial directa con el motor de matching y el pipeline de negociaciones.

- [ ] **Fase 2.4: Generador de Ficha Comercial & Dossier Ejecutivo de Inversión en PDF**
  - [ ] Diseño y maquetación de One-Pager / Teaser Comercial imprimible y descargable en formato PDF de alta fidelidad.
  - [ ] Inclusión de ficha técnica de lote, croquis/ubicación satelital, resumen de parámetros distritales y análisis económico preliminar (incidencia estimada de suelo).
  - [ ] Modal de previsualización e impresión directa para comités de inversión.

---

## 🟢 ESTADO DEL ENTORNO
* **Servidor Dev:** `http://localhost:3000` (Activo y corriendo en background).
* **Compilación TS:** 0 errores (`npx tsc --noEmit` verificado).
* **Build Producción:** 100% exitoso (`npm run build` con 17/17 páginas compiladas).
* **Base de Datos:** Supabase PostgreSQL Cloud + PostGIS (`EPSG:4326`) activa con 8 tablas relacionales auditadas (11/11 PASS).
